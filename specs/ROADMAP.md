---
created: 2026-09-16
updated: 2026-09-22
---

# ERP 全域路线图（ROADMAP）

> 定位：ERP **全域**（内核 + 外围系统）的**路线索引**，用于排定模块边界、阶段、序号与依赖。
> 单一事实源：功能需求仍以各 `specs/<序号>-<feature>/` 为准（`AGENTS.md` §2.1）；本文件只写「边界在哪 / 做什么 / 为何按此顺序 / 依赖谁」，**不复制规格正文**（§10）。
> 序号 = 创建顺序（§2.1，三位定长、`001` 起，仅作目录排序，不属于功能名）；阶段内「顺序」即推荐实现顺序；确需插队时新功能取更大序号并在本表注明。
> 状态取值：`已实现`（`tasks.md` 全勾）/ `实现中`（已开工，`tasks.md` 部分勾选）/ `规格已起草`（三件套就绪，未开工）/ `待起草`（开工前按 §2.2 起草）。
> 维护：边界 / 阶段 / 序号 / 状态变化时更新本表；本表变更**不算**任何 spec 的正文变更（§2.5，不刷新其 `updated`）。
> 沿革：`2026-09-16` 初建为「进销存（`erp-*`）路线」；`2026-09-20` 升级为 ERP 全域（并入 HCM / CRM，判定 WMS 等外围边界，重排为 P1–P6 阶段），并确认**纯贸易、不做生产制造**及范围 / 财务深度 / 组织维度等决策（见 §6.9）；`2026-09-22` `028` 后端交付（权限点全量标注 + 默认拒绝、角色域用例、迁移回填，`40300` 转生产码），状态转「实现中」，业务码占用推进至 `40175`；同日前端（菜单 / 按钮过滤、角色管理页、`/403`）与 e2e（131 通过）交付，状态转「已实现」；同日 `029` 交付（`012`–`028` 全部写用例接入 `IAuditLogger`、范围表守卫测试、`/api/audit-logs` 只读查询、前端操作日志页与 e2e），状态转「已实现」；同日 `030` 交付（部门树 / 岗位字典 / 员工档案 + 员工↔账号绑定、导出，`departments.*` / `positions.*` / `employees.*` 权限点续行、菜单「系统」分组追加三项、写用例接入操作日志、组织人事 e2e），状态转「已实现」，业务码占用推进至 `40148`。

## 1. 范围定义：ERP 内核 与 外围系统

一句话边界：**ERP 管"账"（钱与物与账务），外围系统管"执行与外延"（客户 / 人 / 作业）**。
业务形态：**纯贸易 / 分销**（买成品卖成品，不改变物料形态）——**不做生产制造**（见 §6.9）。

| 系统 | 定位 | 与本项目关系 | 路线处置 |
|---|---|---|---|
| **ERP 内核** | 财务 · 供需链 · 主数据 · 平台 | 现状主体 | **本路线主线** |
| **HCM**（人力资本） | 组织 / 岗位 / 员工 / 考勤 / 薪酬 | 现仅有登录账号（`009`），无组织与员工 | **并入主线**：组织 · 岗位 · 员工先行；考勤 · 薪酬后置 |
| **CRM**（客户关系） | 售前 / 客户经营 / 服务 | 交易侧（客户 · 订单 · 回款 · 客户价）已由内核覆盖 | **并入主线**：报价先行；线索 · 商机 · 服务后置 |
| **WMS**（仓储执行） | 库位 / 波次 / 拣货 / 装箱 / RF | 全无 | **独立立项**，不进主线，以单据接口集成 |
| **SRM**（供应商协同） | 询价 / 对账 / 门户 | 全无 | 后期 / 独立 |
| **MES**（制造执行） | 派工 / 报工 / 设备采集 | 全无 | **不做**（纯贸易无生产，见 §6.9） |
| **PLM**（产品数据） | BOM / 图文档源头 | 全无 | **不做**（纯贸易无生产，见 §6.9） |
| 通用 OA / BI | 通用办公审批 / 深度分析 | 轻量审批由内核审批域覆盖；业务报表已由 `025` 覆盖 | 不单独做 / 后期 |

**边界判定要点**（防混淆）：

- **ERP 库存 ≠ WMS**：ERP 管库存台账（数量 / 金额 / 成本）与多仓 / 调拨 / 批次；WMS 管库位与作业执行。`038` / `039` / `040` 是 ERP 库存精细化，**不等于** WMS。
- **ERP 生产计划 ≠ MES / PLM**：前者管 BOM / MRP / 生产订单（计划与账）；后二者管工序执行与产品数据源头。**本路线为纯贸易，生产制造 / MES / PLM 均不做**（见 §6.9）。
- **组织模型是 HCM 与权限 / 审批的交汇点**：`028` 数据级权限与 `042` 审批流依赖组织 / 岗位 / 员工，须与组织模型一并设计（`028` §5「不做组织模型」据此修订）。

## 2. 模块地图（内核 5 层 + 外围）

```
ERP 内核
├─ L5 分析与决策   经营报表 / 财务三大报表 / BI
├─ L4 财务         总账GL · 应收应付 · 资金出纳 · 成本 · 发票税务 ·（预算·固资）
├─ L3 供需链       采购 · 销售 · 库存
├─ L2 主数据       物料 · 往来 · 仓库 · 价格政策 · 财务主数据
├─ L1 平台底座     组织 · 权限 · 审计 · 审批 · 通知 · 集成
└─ 人力域(HCM)     组织岗位 · 员工档案 ·（考勤 · 薪酬 · 招聘绩效）
外围系统（按需，独立立项）
├─ CRM   客户与售前 / 服务
├─ WMS   仓储作业执行
└─ SRM   供应商协同
（MES 制造执行 / PLM 产品数据 / 生产制造：纯贸易不做，见 §6.9）
```

> 每层的功能需求仍以对应 `specs/<序号>-<feature>/` 为准；本图为**边界与归属**，不复制正文。

## 3. 覆盖矩阵（现状 vs 缺口）

| 层 / 域 | 现状 | 已实现 / 已起草 spec | 缺口 |
|---|---|---|---|
| L1 平台 · 权限 / 审计 | 权限点全量标注 + 默认拒绝 + 前端菜单 / 按钮过滤（`028`）；`012`–`028` 全部写路径留痕 + 只读查询页（`029`） | `028` / `029`（均已实现） | 数据级权限（依赖组织 / 员工 `030`） |
| L1 平台 · 审批 / 通知 | 无 | `041`（起草） / `042`（起草） | — |
| L1 平台 · 组织（HCM 前置） | 登录账号（`009`）+ 部门 / 岗位 / 员工档案（`030`） | `009` / `030` | — |
| 人力域 · HCM 考勤 / 薪酬 | 无 | `044`（起草） | 招聘 / 绩效后置 |
| L2 主数据 · 物料 / 往来 / 分类 | 有 | `012` / `013` / `017` | 计量单位（`035`，已起草）；编码规则（后置） |
| L2 主数据 · 仓库 | 单仓 | `014` | 多仓（`038`） |
| L2 主数据 · 价格政策 | 无 | — | 协议价 / 账期 / 信用（`036`） |
| L2 主数据 · 财务主数据 | 无 | — | 科目 / 税率（`031`，已起草）；银行账户（`034`） |
| L3 采购 | 基本完整 | `015` / `021` / `024` | 请购 · 采购结算单（未纳入路线，按需评估） |
| L3 销售 | 基本完整 | `016` / `022` / `024` | 报价单（`037`，已起草，CRM 前段） |
| L3 库存 | 基本完整 | `014` / `019` / `020` | 多仓 / 调拨 / 批次（`038` / `039` / `040`） |
| L4 财务 · AR/AP / 成本 | 有 | `023` / `026` | — |
| L4 财务 · 发票税务 | 无 | `032`（起草） | — |
| L4 财务 · 总账 GL | 无 | — | 凭证 / 期间 / 结账 / 资产负债表 / 利润表（`033`，已起草） |
| L4 财务 · 资金出纳 | 无 | — | 银行账户 / 资金日记账（`034`，已起草） |
| L5 分析 · 报表 | 业务报表 | `025` | 财务三大报表（随 GL） |
| 外围 · CRM | 交易侧具备 | `013` / `016` / `023` / `036` | 售前（`043`）/ 服务（`045`），已起草 |
| 外围 · WMS / SRM | 无 | — | 独立立项（不进本主线） |
| 外围 · MES / PLM / 生产制造 | 无 | — | **不做**（纯贸易无生产，见 §6.9） |

## 4. 路线

### 4.1 已完成（`019`–`027`）

> 均为「进销存」阶段交付，构成 ERP 内核的供需链与部分财务能力。

| 序号 | feature | 目标（一句话） | 依赖 |
|---|---|---|---|
| `019` | `erp-stock-movement` | 库存变动流水：谁在何时把哪个商品改了多少、来自哪张单 | — |
| `020` | `erp-stock-take` | 期初建账 + 库存盘点（账实差异一次性调整，写流水） | `019` |
| `021` | `erp-purchase-return` | 采购退货单（红字，退回供应商，库存减少并写流水） | `019` / `015` |
| `022` | `erp-sale-return` | 销售退货单（红字，客户退回，库存回增并写流水） | `019` / `016` |
| `023` | `erp-settlement` | 收付款单 + 应收应付：已结算金额、部分结算与往来对账 | `015` / `016` |
| `024` | `erp-order-flow` | 两段式单据：采购订单 → 入库单、销售订单 → 出库单，含部分收货 | `015` / `016` / `021` / `022` |
| `025` | `erp-report` | 进销存报表 / 库存余额表 / 采购 / 销售汇总；重构菜单分组 | `019` |
| `026` | `erp-cost` | 成本核算（移动加权平均）与销售毛利 | `019` / `020` / `025` |
| `027` | `erp-export` | 列表导出 Excel + 单据打印（含文件下载契约例外） | `012`–`026` |

### 4.2 阶段路线（P1–P6）

> 当前**无待起草模块**（`028`–`045` 已全部起草）；后续新增功能序号从 `046` 起按创建顺序分配（§2.1）。

| 阶段 | 主题 | 顺序 | 序号 | feature | 目标（一句话） | 依赖 | 状态 |
|---|---|---|---|---|---|---|---|
| **P1 平台地基** | 权限 · 留痕 · 组织 | 1 | `028` | `erp-rbac` | 权限体系（菜单 + 按钮级），启用预留码 `40300` | `012`–`027` | 已实现 |
| | | 2 | `029` | `erp-audit-log` | 业务操作审计日志（单据 / 档案写操作留痕） | `012`–`028` | 已实现 |
| | | 3 | `030` | `erp-org-employee` | 组织 / 部门 / 岗位 + 员工档案（HCM 前置，支撑权限与审批） | `009` | 已实现 |
| **P2 财务闭环** | 业财一体 | 4 | `031` | `erp-finance-master` | 财务主数据：会计科目 / 税率 | — | 规格已起草 |
| | | 5 | `032` | `erp-invoice` | 发票登记（进项 / 销项）+ 单据关联 | `015` / `016` / `021` / `022` / `023` | 规格已起草 |
| | | 6 | `033` | `erp-general-ledger` | 总账：凭证 / 期间 / 期末结转；单据自动生成凭证；三大报表 | `015` / `016` / `023` / `026` / `031` | 规格已起草 |
| | | 7 | `034` | `erp-cash` | 资金出纳：银行账户 / 资金日记账 | `023` / `033` | 规格已起草 |
| **P3 主数据与政策** | 底座与闸门 | 8 | `035` | `erp-uom` | 计量单位字典 + 商品单位引用（换算 / 编码规则后置） | `012` | 规格已起草 |
| | | 9 | `036` | `erp-partner-price` | 客户价格策略、账期与信用额度 | `013` / `023` | 规格已起草 |
| | | 10 | `037` | `erp-quotation` | 报价单（销售订单前段，CRM 轻量入口） | `012` / `013` / `024` | 规格已起草 |
| **P4 库存精细化** | 规模经营 | 11 | `038` | `erp-multi-warehouse` | 多仓库：库存按仓、单据带仓、按仓安全库存 | `012`–`022` | 规格已起草 |
| | | 12 | `039` | `erp-transfer` | 调拨单（转出仓 → 转入仓；一期不含在途，见 §6.8） | `019` / `026` / `038` | 规格已起草 |
| | | 13 | `040` | `erp-batch-expiry` | 批次 / 保质期（序列号不做，见 §6.8） | `038` / `039` | 规格已起草 |
| **P5 流程增强** | 运营化 | 14 | `041` | `erp-stock-alert` | 库存预警通知（定时扫描 + 站内信） | `019` / `028` / `038` / `040` | 规格已起草 |
| | | 15 | `042` | `erp-approval` | 大额单据审批流（单级阈值；改变单据生效时点） | `028` / `041` / `030`（组织/员工） | 规格已起草 |
| | | 16 | `043` | `erp-crm-presale` | CRM 售前：线索 / 商机 / 活动 | `013` / `037` | 规格已起草 |
| **P6 外围增强** | 按需 | 17 | `044` | `erp-hcm-payroll` | 考勤登记 / 月度薪酬（招聘绩效后置） | `030` | 规格已起草 |
| | | 18 | `045` | `erp-crm-service` | CRM 服务工单 / 客户经营 | `043`（CRM 售前） | 规格已起草 |
| | | — | — | `WMS` / `SRM` | 独立系统，独立立项（见 §1） | — | 范围外 |

## 5. 阶段划分与排序理由

| 阶段 | 包含 | 为什么在这一阶段 |
|---|---|---|
| **P1 · 平台地基** | `028`、`029`、组织 / 员工 | 完整 ERP 的前置是"有事可管、有据可查"：权限与审计决定谁能做什么、做过什么；组织 / 员工是数据级权限与审批流的共同前置，早做免得返工（`028` 原排除组织模型，据此修订） |
| **P2 · 财务闭环** | 财务主数据、`032`、总账 GL、资金出纳 | **从"进销存"升级为 ERP 的分水岭**：先把发票纳入，再建总账让单据自动生成凭证、出三大报表；财务主数据（科目 / 税率 / 账户）是总账前置；资金出纳补齐 `023` 之外的现金视角 |
| **P3 · 主数据与政策** | 单位 / 编码规则、`036`、报价单 | 主数据完整度决定后续所有单据的规范度；`036` 给销售加"取价 + 授信"闸门；报价单承接 CRM 前段 |
| **P4 · 库存精细化** | `038`、`039`、`040` | 多仓、调拨、批次属规模 / 行业特化，**默认单仓经营时不做**；启用多仓须先确认确有多点经营（见 §6.2） |
| **P5 · 流程增强** | `041`、`042`、CRM 售前 | 预警依赖定时任务与权限；审批依赖组织与权限；CRM 售前依赖报价与客户主数据。管理成熟后才需要 |
| **P6 · 外围增强** | HCM 薪酬、CRM 服务、WMS / SRM | 人力 / 服务按需；`WMS` / `SRM` 为独立系统，不进主线。**生产制造 / MES / PLM 已确认不做**（纯贸易，见 §6.9） |

> 已交付的「进销存」功能见 §4.1；本表只描述**未实现**部分的阶段划分。

## 6. 跨功能前置决策（后续规格共同遵守）

> 错误码与权限点的**具体取值仍在各功能 `design.md` 定义**（`AGENTS.md` §4.2 / §10 单一事实源）；本节只登记「占用区间与下一个可用值」，避免新增功能撞码。当前：业务码已占用 `40101`–`40175`（`40112`–`40117` 属 `023` / `024`；`40138`–`40172` 属 `030`–`045`；`40173`–`40175` 属 `028`），其中 `030` 已用 `40138`–`40148`（**其区间内下一个可用 `40149`**），全局**下一个可用 `40176`**；权限点清单唯一来源为 `specs/028-erp-rbac/design.md` §0.2（后续规格在其表内续行）。

### 6.1 库存流水是所有下游能力的地基

`019` 的流水表是报表（`025`）、成本（`026`）、盘点（`020`）、多仓（`038`）、库存预警（`041`）的共同数据源。任何"新增一种库存变动来源"的功能（退货、调拨、盘点）都必须在**同一事务内**追加流水，不允许出现"库存变了但无流水"。

### 6.2 多仓库：默认不做，接入时有既定路径（修正早期判断）

- 现状**按单仓设计**（`Inventory` 唯一键 `ProductId`）。是否上多仓取决于是否存在两个以上物理存放点且需要分别掌握各点数量、需要调拨。
- **修正**：早前"流水表必须预留 `WarehouseId` 列"的判断过重，现改为**不预留**。理由：PostgreSQL 加列（含 `NOT NULL` + 默认值）是元数据操作，且流水表是纯追加表、无涉及仓库维度的唯一索引，多仓接入时为"增量迁移加列 → 回填默认仓 → 补索引"即可，不构成返工；反过来现在放一个恒为空的列会污染所有写入路径的语义。
- `038` 落地时的既定动作（届时写进该 spec）：`Inventory` 唯一键改 `(ProductId, WarehouseId)` + 回填默认仓；`StockMovements` 增 `WarehouseId` 并回填；采购 / 销售单增仓库字段；库存不足判定与安全库存按仓；调拨单独建单（`039`）。

### 6.3 结算：`SettlementStatus` 升级路径

`015` §5 已预留：`023` 把单据上的 `0/1` 状态位替换为「已结算金额 + 状态（未结 / 部分 / 结清）」，历史数据按原状态位迁移（`1 → 已结算金额 = 总额`）。收付款单与应收应付以往来单位为主键聚合，单据与收付款明细建立核销关系。

### 6.4 单据编辑：不单独做，由两段式（`024`）覆盖

一步式单据下"改单"等价于"作废重开"（`015` §5 已定），无需独立编辑功能。`024` 引入两段式后，**订单可编辑、出入库单仍不可编辑**，编辑诉求即被覆盖。

### 6.5 权限与组织：`028` 落地时统一补齐

`028` 之前新增的菜单与接口不做权限判断（"登录即可见"）；`028` 落地时统一为既有功能补齐菜单级 / 按钮级权限，并在前端隐藏无权限入口（`40300` 从预留码转为生产码）。**组织 / 部门 / 岗位 / 员工模型与 `028` 的开发同期或先行**，作为数据级权限与 `042` 审批流的前置；`028` §5「不做组织模型」据此修订（`2026-09-20` 升级 ERP 时确定）。

> **进度（`2026-09-22`）：`028` 已实现**——后端全部 Controller 动作已标注权限点并由集成测试守卫（漏标即拒绝）、`40300` 已由预留码转为生产码（`AGENTS.md` §4.2 同步）、角色域用例与迁移回填完成（`dotnet test` 605 通过）；前端菜单 / 按钮过滤、角色管理页、`/403` 页已落地，`npm run test:e2e` 131 通过。**既有功能「登录即可见」的时代结束**：无权限入口隐藏、无权限动作返回 `40300`。剩余缺口为数据级权限，依赖 `030` 组织 / 员工模型。

### 6.6 规格与规则联动

- 新增功能按其"改动类型"消费既有交互约定正文（对照表见 `.codebuddy/CONTEXT.md` §6），不重复定义。
- 新功能若改变既有规格的既定决策（例如 `019` 修订 `014` 的"库存查询页无操作列"），必须**同时**把双方 `design.md` 中受影响的正文改写为**最终态**，并在文件头部留**一行**演进指针（形如 `> **演进（<改写方>）**：<一句话说明改了哪几项>，决策与判据见 specs/<改写方>/design.md §X`）。**禁止**保留已被推翻的旧表述或「原始设计记录」块——旧值只查 git 历史，规格正文只反映现状。

### 6.7 单号前缀分配（全域唯一来源）

| 单据 | 前缀 | 引入 / 变更规格 | 备注 |
|---|---|---|---|
| 采购订单 | `PO` | `024` | 由现有一步式「采购单」前缀让位给订单 |
| 采购入库单 | `GR` | `015` / `024` | 现有一步式采购单；`024` 重命名表并改写历史单号前缀（`PO` → `GR`） |
| 采购退货单 | `PR` | `021` | |
| 销售订单 | `SO` | `024` | 由现有一步式「销售单」前缀让位给订单 |
| 销售出库单 | `GI` | `016` / `024` | 现有一步式销售单；`024` 重命名表并改写历史单号前缀（`SO` → `GI`） |
| 销售退货单 | `SR` | `022` | |
| 收款单 | `RC` | `023` | |
| 付款单 | `PY` | `023` | |
| 库存盘点单（含期初建账） | `ST` | `020` | 单号形式 `<前缀> + yyyyMMdd + 4 位序号` |
| 调拨单 | `TR` | `039` | 转出仓 → 转入仓（一期一步式，不含在途） |

- 新增单据类型前先查本表，避免与他域前缀冲突；生成机制见 `specs/015-erp-purchase/design.md` §3.6（前缀参数化）。
- 规划中单据（报价单 `QT`、记账凭证等）待其规格起草时在本表续行；不预先占位。

### 6.8 已确认决策（2026-09-17）

> **状态：已全部确认** —— 用户指示「按起草建议执行」：`024` 采用其 `design.md` §5 推荐路径（表 / 实体 / 接口重命名 + 历史单号前缀改写）；`039` 一期不含在途；`040` 不做序列号；`025` 菜单改为多顶级分组；`027` 采用文件下载契约例外（实现时在 `AGENTS.md` §4.1 登记）。下表保留各决策与影响说明，供实现时对照。

| 决策 | 规格 | 说明 |
|---|---|---|
| 既有单据表重命名 + 历史单号前缀改写 | `024-erp-order-flow` §5 | 涉及表名、接口路径与用户可见单号的不可逆变更（`PurchaseOrders` → `PurchaseReceipts` 等）；**确认后才开工**，未确认时 `024` 整体阻塞 |
| 调拨一期**不含在途** | `039-erp-transfer` §5 | 路线图原文「含在途」；起草时裁剪为一步式（转出即转入），理由：在途需状态机与两段式执行单，宜与 `024` 模板统一。**如需一期含在途须回改需求**（spec §5 已记演进路径） |
| 批次规格**不做序列号** | `040-erp-batch-expiry` §5 | 路线图原文「批次 / 保质期 / 序列号」；序列号是逐件粒度与全链路追溯，属另一套模型，建议独立规格。**如需一期含序列号须回改需求** |
| 菜单改为**多顶级分组** | `025-erp-report` §0.2 | 进销存分组项数已超 10（`024` 预告）；`025` 落地时把菜单改为「基础档案 / 采购 / 销售 / 库存 / 资金 / 报表 / 系统」多组（不嵌套子菜单），会改变既有菜单路径与 e2e helper 映射。**如希望保持两组平铺须回改需求** |
| 导出接口**不进统一响应** | `027-erp-export` §0.1 | 文件下载类接口成功时返回二进制流（非 `{code,message,data}`），需在 `AGENTS.md` §4.1 登记例外条款；**确认后才按例外实现**（否则改为 JSON + base64 包装） |
| 权限点数与错误码占用 | `028` §0.2 / 各规格 §3.2 | `025`–`045` 已占用业务码 `40118`–`40172`，`028` 追加 `40173`–`40175`（下一个可用 `40176`）；权限点随 `028` §0.2 清单续行。无需确认，仅备查 |

### 6.9 本轮范围决策（2026-09-20）

> **状态：已全部确认（2026-09-20，按建议执行）** —— 用户确认「按方案落盘」「纯贸易，不生产」，并指示「先按建议更新一版」；下列各项即为定稿，后续如需调整在此修订。

| 决策 | 结论 | 影响 |
|---|---|---|
| **业务形态 / 生产制造** | **纯贸易、不生产** | `erp-manufacturing` 及 BOM / MRP / MES / PLM 出局；`012` 无需预留"原料/半成品"类型，`026` 成本模型维持"纯购进移动加权" |
| **目标范围** | ERP 内核 + HCM（组织 / 员工先做）+ CRM（轻量并入）；WMS / SRM 独立立项 | 决定 P1 组织模块与 P5 / P6 的 CRM 边界 |
| **财务深度** | 业务财务一体（凭证 + 三大报表）；预算 / 固定资产后置 | 决定 `erp-general-ledger` 范围 |
| **组织维度** | 单主体起步，多公司 / 合并报表后置 | 决定总账与权限是否含多组织 |
| **`028`–`045` 处置** | 全部保留，按 §4.2 归入 P1–P6 | 不冻结任何已起草规格 |

## 7. 范围外（明确不做）

- **生产制造（BOM / MRP / 生产订单）**：用户确认**纯贸易、不生产**（`2026-09-20`），不做。
- **MES / PLM**：制造执行 / 产品数据源头，随"不做生产"一并出局。
- **WMS 级仓储作业**（库位 / 波次 / 拣货 / 装箱 / RF）：属仓储执行系统，**独立立项**，不进本主线（见 §1）。
- **SRM**：供应商协同（询价 / 对账 / 门户），按需独立立项。
- **序列号 / 逐件追溯**：`040` 只做批次 + 保质期；一物一码、逐件出入库核对与全链路追溯属另一套模型，未纳入本路线（如需另立规格）。
- **多公司 / 多组织 / 合并报表**（本阶段）：先单主体经营；多组织随 P6 后置。
- **预算 / 固定资产 / 费用报销**：本阶段总账只做"业务财务一体"，不做预算编制、固资折旧、费用报销。
- **深度税务**：发票只做登记与关联（`032`），不做开票接口对接与税控。
- **电商 / 门店前台**：线上商城、POS、会员积分。
- **通用 OA / 深度 BI**：轻量审批由内核审批域覆盖，业务报表由 `025` 覆盖，不单独做。

> 注：**财务级账务（总账 / 凭证 / 期末结转）由原「范围外」改为 P2 范围内**（`2026-09-20` 升级 ERP 时翻案，见 §4.2 / §6.9）。

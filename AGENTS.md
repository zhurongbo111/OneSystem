# AGENTS.md — 项目总体规则

> 本文件是项目总体规则（总则），AI 会话自动全文加载。
> 职责划分：本文件是**工程约定**的唯一事实源；`specs/<序号>-<feature>/` 是**功能需求**的唯一事实源。两者冲突时以规格为准，但应提示用户核对规格是否有误。
> 专项规则（`.codebuddy/rules/<端>/RULE.mdc`）只写本文件的落地细节、不重复总则条款；引用时标注章节（如 §4.6）。总则与专项规则如有出入，以本文件为准并提示核对。
> 实现新功能时：先读 `specs/<序号>-<feature>/` 规格，再按需加载对应端的规则文件。

## 1. 项目概述

| 项 | 约定 |
|---|---|
| 仓库形态 | 单仓库 monorepo：`frontend/` + `backend/` |
| 前端 | Vue 3 + Arco Design（细则见前端规则） |
| 后端 | .NET 8 + ASP.NET Core + EF Core（细则见后端规则） |
| 数据库 | PostgreSQL |
| 开发模式 | 规格驱动开发（SDD） |

## 2. 规格驱动开发（SDD）

### 2.1 规格目录

- 每个功能一个 `specs/<序号>-<feature-name>/`（整体 kebab-case，与功能一一对应），固定三文件，**不得新增其他层级**：
  - `requirement.md` — 需求规格：背景、目标、功能点、验收标准；
  - `design.md` — 设计规格：API 设计、数据模型、前端交互、技术决策；
  - `tasks.md` — 任务清单：可勾选（`- [ ]`）的任务项。
- **序号**：三位十进制、定长、`001` 起，**表示既定实现顺序**（新建功能取「现存最大序号 + 1」；为对齐实现顺序可整体重排，重排须同步更新全部引用，见 `specs/ROADMAP.md`）；**仅作目录排序**，不属于功能名；`specs/` 按名称排序即实现顺序。需要单指功能名时（分支名 / 路由 / 功能域对齐等）一律用 `<feature-name>`，**不带序号**（见 §8.1）。
- **路线索引**：功能组的后续规划（批次、序号、依赖、状态与跨功能前置决策）集中记在 `specs/ROADMAP.md`（单文件，**不属功能规格、无三件套**）；规划变化更新该文件，功能需求仍以各 `<序号>-<feature>/` 为准（单一事实源见 §10）。

### 2.2 工作流（强制）

1. **需求先行**：任何功能（含小改动）必须先创建或更新 `specs/<序号>-<feature>/requirement.md`。
2. **设计**：需求确认后写 `design.md`；新增/变更的接口、数据库改动、前端页面必须在此定义。
3. **任务拆分**：设计确认后拆分为 `tasks.md` 中可勾选的任务项。
4. **实现**：严格按 `tasks.md` 顺序实现；执行每个任务前先重读规格相关章节。
5. **勾选**：任务完成后在 `tasks.md` 勾选；全部完成即功能完成。
6. **变更**：开发中需求变化，先改规格、再改代码；禁止绕过规格直接改代码。

### 2.3 规则

- 规格是功能的唯一事实源，代码必须始终与规格一致；实现前必须先读 `specs/<序号>-<feature>/` 三个文件，规格缺失时先提示补规格、不直接写代码。
- **冲突裁决顺序**：① 规格 ↔ 总则 → 以规格为准并提示核对规格（见文首）；② 规格 ↔ 专项规则 → **以规格为准**，并当场修正规则或规格使两者一致，不得两边各写一套；③ 总则 ↔ 专项规则 → 以本文件为准。
- 规格与规则各为单一事实源：**规则不得复制规格正文，规格不得复制规则正文**（只能指针），见 §10。

### 2.4 实现前最小读取集（强制）

- AI 动手写代码前，按下述固定集合读取，**禁止**为"了解项目"做全仓或超范围代码探索：
  1. `.codebuddy/CONTEXT.md`（项目结构摘要，替代代码探索）；
  2. `specs/<序号>-<feature>/design.md` + `tasks.md`（**新建规格或需求有变时**，按 §2.2 步骤 1 先读 / 先写 `requirement.md`）；
  3. 对应端的专项规则（backend → `.codebuddy/rules/backend/RULE.mdc`，frontend → `.codebuddy/rules/frontend/RULE.mdc`）；
  4. 仅本次要改动的目标文件（含直接调用的既有文件），不做目录级遍历。
- `.codebuddy/CONTEXT.md` 维护要求：项目结构（项目 / 模块 / 实体 / 功能 / 关键文件位置 / 命令 / 端口）发生变化时，须随当次变更同步更新并一起提交；摘要与实际不一致时以代码和 `specs/` 为准并顺手修正摘要。
- **各端的端内固定读取集**见专项规则：前端 → 前端规则 §2.1，后端 → 后端规则 §3.1（只列该端的参照实现 / 既有同类文件 / 测试支撑，同样禁止目录遍历）。

### 2.5 规格元数据（强制）

- 每个 spec 的三个文件（`requirement.md` / `design.md` / `tasks.md`）顶部必须带 YAML frontmatter，含 `created`（首次创建日期）与 `updated`（正文最后一次变更日期）两个字段，格式 `YYYY-MM-DD`，三处取值必须一致。
- **新建 spec**（§2.2 步骤 1）：`created = updated =` 当日，三文件一并写入。
- **变更 spec**（§2.2 步骤 6）：改动正文时同步刷新该 spec 三文件的 `updated`。
- **表述层的等价改写不算正文变更**：措辞压缩、重复表述改为指针、追加块并入对应章节、已勾选任务项的变更记录收敛、元数据 / 格式 / 路径迁移——均**不得**刷新 `updated`，避免时间序失真（`updated` 只表示**需求 / 决策实质变化**的日期）。
- **排序**：spec 清单一律按目录序号（= 创建时间正序）呈现；确需按更新时间排列时，按 `updated` → `created` → 序号倒序。

## 3. 目录结构

- 顶层：`AGENTS.md`（本文件）、`.codebuddy/`（`CONTEXT.md` + `rules/`）、`specs/`（功能规格）、`frontend/`、`backend/`。
- `frontend/`、`backend/` 内部结构与关键文件位置见 `.codebuddy/CONTEXT.md` §2 / §3（本文件不重复，避免两处维护漂移）。

## 4. API 契约（前后端共同遵守）

### 4.1 统一响应

所有接口统一返回：

```json
{ "code": 0, "message": "success", "data": {} }
```

- `code = 0` 表示成功；非 0 一律为错误，前端统一提示 `message`。
- 前端请求封装统一解包 `data`，业务代码只处理数据本身。
- **例外：文件下载类接口**（当前仅 `027-erp-export` 的导出接口）：成功时直接返回二进制文件流（不套统一响应），**参数非法 / 服务端异常仍返回统一响应 JSON**，前端按 `Content-Type` 区分；具体契约与前端分流实现见 `specs/027-erp-export/design.md` §0.1，新增下载类接口必须先在该节登记。

### 4.2 错误码

| code | 含义 |
|---:|---|
| 0 | 成功 |
| 40000 | 参数错误 |
| 40100 | 未登录或 token 无效 |
| 40300 | 无权限 |
| 40400 | 资源不存在 |
| 50000 | 服务内部错误 |

业务错误码扩展在各功能 `design.md` 中定义，禁止随手新增。

- `40300` 为**无权限**（`028` 起由预留码转为生产码）：后端**默认拒绝**——无权限访问、以及未标注 `[RequirePermission]` 的动作一律返回 `40300`（HTTP 200），未登录走 `40100`；前端按「无权限」提示，处置方式同 `40000`（统一 `Message.error`）。权限点清单唯一来源见 `specs/028-erp-rbac/design.md` §0.2，白名单见其 §0.4。

### 4.3 分页

分页接口使用 Query 参数 `page`（从 1 起）、`pageSize`；`data` 结构：

```json
{ "items": [], "total": 0, "page": 1, "pageSize": 20 }
```

- 取值约定：`page` ≥ 1；`pageSize` **默认 20、上限 100**（由后端 Validator 校验，越界返回 `40000`；前端列表页 `pageSizeOptions` 见前端规则 §5）。

### 4.4 命名与序列化

- 资源用名词复数，如 `/api/users`；不使用动词作资源路径。
- 后端 JSON 序列化采用 camelCase，与前端 DTO 类型一一对应。

### 4.5 更新类接口（PUT）字段语义

- **全量覆盖**：更新接口的可选字段一律采用全量覆盖语义——JSON 缺字段、空串、纯空白一律视为清空（落库 `null`）；有值则 `Trim` 后存储。前端编辑表单始终全量提交，与该语义一致。
- **不可改字段**：接口不接受、保持原值的字段（如名称 / 编码）须在 `design.md` 明确标注「不可改」，请求体不得包含该字段。
- **例外**：采用「缺失保持原值」（部分更新）语义的接口，须在 `design.md` 显式声明并说明理由，禁止默认行为分叉。

### 4.6 认证（JWT）

- 登录成功后由后端签发 JWT；前端后续请求统一携带 `Authorization: Bearer <token>` 请求头。
- 后端全局校验 token（登录等白名单接口除外）；校验失败统一返回 `code: 40100`。
- token 过期 / 失效：前端统一处理 40100，清除本地凭证并跳转登录页。
- 签发与校验实现见后端规则；token 存储与携带见前端规则。

## 5. 通用编码约定（前后端共同遵守）

- 提交信息使用 Conventional Commits：`feat|fix|docs|refactor|test|chore(scope): 描述`。
- 异常处理：禁止吞异常；错误日志必须包含上下文（含 traceId）。
- 语言：代码标识符使用英文；注释与文档使用中文。

## 6. 测试策略（总述）

- 后端：单元测试（xUnit），重点覆盖 Handler 业务逻辑，细则见后端规则。
- 前端：不单写单元测试；集成测试用 Playwright e2e（`frontend` 下 `npm run test:e2e`，直连 dev 后端、不打 mock），细则见前端规则。
- **前端 e2e 覆盖（强制）**：凡涉及前端改动的功能，交付前必须有 e2e 覆盖改动的用户可感知行为；无 e2e 视为未完成，不得勾选对应 `tasks.md` 任务。
- **跑通测试（强制）**：前后端都有改动时，`dotnet test` 与 `npm run test:e2e` 必须都跑通；只改一端时至少跑被改动端。测试未通过不得声明功能完成。

## 7. 环境与可观测性

- 环境只分 `dev`（开发）/ `prod`（生产）两个，后续按需扩展。
- 配置原则：非敏感配置放配置文件并按环境区分；敏感配置（数据库连接串、JWT 密钥等）只从环境变量读取，禁止硬编码、禁止入库。
  - **dev 例外**：本地开发库的连接串允许明文写入 `appsettings.Development.json`（**仅限本地 / 开发库**，不得为生产或共享环境凭据）；prod 及任何共享环境一律只从环境变量读取。
- 可观测性：后端集成 OpenTelemetry（Tracing + Metrics），日志使用 NLog 对接默认 `ILogger<T>` 框架，细则见后端规则。

## 8. Git 分支与提交

### 8.1 分支模型（轻量）

| 分支 | 用途 | 规则 |
|---|---|---|
| `main` | 长期分支，始终可发布（对应 prod） | 受保护：只接受 merge（带 PR 或经确认的 merge）；禁止直接 push / force push |
| `feature/<功能名>` | 功能开发 | 从 `main` 切出；功能名 kebab-case，与 `specs/<序号>-<feature>/` 中的 `<feature>` 一致（**不含序号**）；完成后合回并删除 |
| `hotfix/<简短描述>` | 生产紧急修复 | 从 `main` 切出，修复合回后立即删除 |

- 同一时间只在一个分支上开发；禁止直接在 `main` 上开发、禁止 force push、禁止改写已推送历史；合并用普通 merge 或 squash merge。
- **待办**：CI/CD 与分支保护尚未建立，`main` 的「受保护」与 §6 的测试门槛当前**靠约定执行**；流水线建立后转为技术强制。

### 8.2 提交规范

- 提交信息遵循 Conventional Commits（见第 5 节）；`scope` 使用 `frontend` / `backend` 或具体模块名。
- 规格文件变更单独提交：`docs(specs): ...`。

## 9. 专项规则索引

| 规则 | 文件 | 应用时机 |
|---|---|---|
| 后端实现规则 | `.codebuddy/rules/backend/RULE.mdc` | 开发 / 修改 backend/ 时 |
| 前端实现规则 | `.codebuddy/rules/frontend/RULE.mdc` | 开发 / 修改 frontend/ 时 |

## 10. 规则维护

- 工程约定变更必须**同时**更新对应文件：总则 → 本文件；专项落地细节 → `.codebuddy/rules/<端>/RULE.mdc`。只改代码或只改规格视为未完成。
- **单一事实源**：规则不得复制规格正文，规格不得复制规则正文，只能指针（`specs/<序号>-<feature>/`）；发现重复时收敛到对应侧并留指针。
- **判据留规则、明细归规格**：规则只保留**判据**（必须遵守什么、违反为何不合格）；查表型明细（阈值表、颜色 / 图标映射表、分区顺序表、命名清单、示例片段）归对应规格 `specs/<序号>-<feature>/design.md` §0「约定正文（唯一事实源）」，规则写指针。判据标准：**只在改动某一类页面 / 模块时才需要的属规格，写代码时任何时候都要遵守的属规则**。
- **只记规律，不列可枚举清单**：规则与 `.codebuddy/CONTEXT.md` 中的文件 / 类型 / 用例清单，凡能由目录枚举得到的，都改为「命名规律 + 指针」（参照 `CONTEXT.md` §2）。
- **不物理拆分规则文件**：专项规则属 §2.4 最小读取集，拆分会增加每次加载项；膨胀时按上两条处理，不新建平行文件。规则过长会挤占每次加载的上下文（经验阈值约 1 万字符），超出时先做下沉 / 压缩。
- 规则文件、本文件、`.codebuddy/CONTEXT.md` 的变更随当次变更一起提交（提交信息按 §8.2）。
- 待办：CI/CD 与部署方式（暂不考虑，后续更新；分支保护待办见 §8.1）。

---
description: 前端实现规则。开发或修改 frontend/ 下的 Vue 3 / Arco Design / TypeScript 代码时应用，含技术栈、目录结构、组件与状态管理约定。
alwaysApply: false
enabled: true
---

# 前端实现规则

> 总则见根目录 `AGENTS.md`（SDD 工作流、API 契约、错误码、通用约定与测试门槛）。本文件只写前端专属实现约定，总则已规定的条款不再重复列出；确需引用时标注章节（如 `AGENTS.md` §4.5 / §6）。
> 开发功能前：先读 `specs/<序号>-<feature>/` 规格（`AGENTS.md` §2.2），再按本文件实现。
> 章节号 §4.5 / §4.6 / §4.7 / §5.2 / §5.5 是历史下沉后**保留的编号**（多份规格按此引用），非笔误，**勿重排**。

## 1. 技术栈

Vue 3 + TypeScript（`<script setup lang="ts">` 组合式 API，**禁止 Options API**）+ Arco Design Vue + Vite + Pinia + vue-router + Axios。

## 2. 目录结构

目录职责与文件位置见 `.codebuddy/CONTEXT.md` §3（本文件不重复，避免两处维护漂移）；`api/` 为接口请求层（统一解包、类型定义），`utils/` 只放无副作用纯函数（按主题一个文件）；`playwright.config.ts` **不自动拉起服务**（见 §10 与 `AGENTS.md` §6）。

### 2.1 实现前最小读取集（强制）

> 通用四项（`CONTEXT.md` / 规格 / 本规则 / 本次目标文件）见 `AGENTS.md` §2.4；**端内参照实现见 `.codebuddy/CONTEXT.md` §3「基准参照」，「改动类型 → 规格 §0」对照表见 §6**（本规则 §4.5 / §4.6 / §4.7 / §5 / §5.2 / §5.5 均为判据 + 指针，正文在各规格 §0）。只读本次要改动的功能域目录（`views/<Domain>/`）与同域 `e2e/<域>.spec.ts`，同域既有同类文件即参照（列表页 → `<Entities>View.vue`，表单 → `*FormDrawer.vue` / `*FormPage.vue`，详情 → `*DetailView.vue`）；禁止遍历 `views/` / `components/` 全目录、读无关功能域、为"参考风格"通读 `src/`。

## 3. 接口层

- 所有接口请求集中在 `src/api/`，按业务域拆分文件（如 `user.ts`）。
- **归属判定**：默认一个功能域一个文件；**从属 / 紧密关联的资源可并入其主域文件**，此类并入**必须在功能 `design.md` 中写明归属与理由**，不得随手乱放。
- 统一封装：基于 Axios 拦截器解包 `{ code, message, data }`；`code !== 0` 时用 Arco `Message.error` 提示 `message` 并 reject。
- 请求参数与响应定义 TS 类型，与后端 DTO（camelCase）一一对应。
- 组件内禁止直接调用 `axios/fetch`，必须经 `src/api/` 模块。

## 4. 组件与页面

- 页面级组件放 `src/views/`，按功能目录组织；可复用组件放 `src/components/`。
- UI 统一使用 Arco Design 组件，不自造样式体系；自定义样式优先使用 Arco 主题变量（`@arco-design` CSS 变量 / 主题定制）。
- 组件 props / emits 必须声明 TS 类型。
- 组件名 PascalCase，文件命名与组件名一致。

### 4.1 页面命名与目录归属（强制）

- `src/views/` 按**功能域**分目录，一个域一个 `<Domain>/`；域的划分须与后端 `Features/<Feature>`、路由前缀、侧边菜单项、e2e spec **四者对齐**——后端 / 规格已独立的域，前端不得挂在别的域目录下。「对齐」指**语义同域、不要求字面同名**（目录用单数域名词、后端用复数资源名、路由用复数资源路径、e2e 用 kebab-case 域名）。
- 域目录内文件一律**平铺**、不套 `components/` 子目录：域内共享的私有组件同样平铺，只有跨功能域复用的通用组件才进 `src/components/`；演示 / 示例页统一收进 `views/Showcase/`，不与业务页混层；无功能域归属的独立页平铺在 `views/` 根。
- 命名单复数（列表页用复数域名、表单 / 详情页用单数实体名，与 `src/api/<entity>.ts` 一致；集合概念名词如 `Inventory` 保留单数）与命名对照实例见 `.codebuddy/CONTEXT.md` §3。

### 4.5 组合式 API 分区书写规范（强制）

> 目的：禁止随意位置写 `ref` / `computed` / `watch` / 函数，保证结构可预期。
> **正文（11 分区顺序表、分区内顺序、分区注释、模板 ref 命名、composable 分区）见 `specs/008-composable-style/design.md` §0**；本节只留判据。

- 顶层声明必须按 §0 的固定分区顺序书写，**只允许缺省，不允许乱序或穿插**（组件 `<script setup>` 与 composable `composables/*.ts` 均适用，两者的分区序列见 §0）。
- 模板 ref 命名一律 `xxxRef`（如 `formRef`）。
- import 分组排序与禁用 `any` 已由 lint 强制；分区顺序、`types` 先于变量、ref 命名**无 lint 兜底**，由 code review 把关。

### 4.6 按钮交互反馈（强制）

> 目的：任何会发起请求的按钮，点击后必须**立即**给出 loading 反馈，且操作进行中不可重复触发。
> **正文（状态命名与绑定表、实现要求、不置 loading 的情形）见 `specs/010-button-loading/design.md` §0**；本节只留判据。

- loading 属于「操作」而非「按钮」：同一异步操作的所有触发入口（按钮 / 回车 / 分页器 / 行内按钮）共用同一个状态；不同操作各用独立状态，**禁止多个按钮共用一个 `loading`**。
- 命名固定：查询类 `loading`、表单提交 `submitting`、弹窗确认 `xxSubmitting`、行内操作 `xxingId`、批量操作 `batchXxing`。
- 触发即置位 + `try/finally` 复位（成功与失败路径都复位）；动作类（提交 / 删除 / 启停）必须防重入，查询类**禁止判空丢弃点击**（用请求序号仲裁）。
- 同步瞬时动作（开抽屉 / 弹窗、路由跳转、本地导出、复制）不置 loading；只有「发起请求并等待结果」才置。
- 按钮 loading 属用户可感知行为，相关功能交付前须有 e2e 断言（`AGENTS.md` §6）。

### 4.7 图标选用（强制）

> 项目并存三套图标库：Tabler（`@tabler/icons-vue`，业务默认）、Lucide（`@lucide/vue`）、Arco Design 自带图标（`@arco-design/web-vue` 的 `Icon*`）。
> **正文（三套库用法、选型优先级表、来源注释、尺寸与线宽）见 `specs/018-icon-showcase/design.md` §0**（示例页 `/components` 图标 tab）；本节只留判据。

- 选型按业务语义逐级回退：Tabler（首选）→ Lucide（Tabler 无对应语义时）→ Arco 自带 `Icon*`（兜底），**禁止反向选择**（不得因偏好跳过 Tabler），不得为凑图标使用语义不符的图标。
- Arco 组件的 `#icon` 插槽（含 §5.2 操作列固定图标）**同样优先 Tabler**；用 Lucide / Arco 时在 import 处加来源注释。
- 同一业务图标全项目同来源；尺寸 / 线宽沿用全局统一样式，不逐处写。

### 4.8 表单组件受控绑定（强制）

> 目的：回填场景（编辑页 / 关联单据带出明细）必须让组件拿到外部值，绑定写错会静默表现为「下拉显示占位符，像数据没查到」。
> 判据只此一条，无对应规格正文（非某类页面特有，写代码时任何受控组件都适用）。

- `a-select` 只用 `v-model` / `:model-value` 绑定值：**它没有 `value` 属性**（只有 `modelValue` / `defaultValue`），写 `:value` 会作为透传 DOM 属性被忽略，`modelValue` 恒为 `undefined`——新建时手选看不出问题（组件内部有状态），编辑回填 / 关联带出时下拉恒显示 placeholder。
- 子表格（明细行）内的受控组件一律「`:model-value` + `@change` 写回」；`a-input-number` 等同理，不得用 `:value`。
- 选项数据源与回填写值必须同源：回填值不在选项列表内时下拉同样显示为空（如仅启用商品的下拉遇到已停用商品），需先确认数据源口径而非改绑定。

### 4.9 组件 props 数值语义以实际安装版本为准（强制）

> 目的：同名 props 在不同组件库（Arco 的 Vue 版 / React 版）语义可能不同，照搬文档或凭直觉传值会**静默**出错（不报错，只是显示值 / 宽度错）。
> 判据只此一条，无对应规格正文（任何使用组件 props 的地方都适用）。

- 比例 / 区间类 props 一律按**当前安装版本**的语义传值：动手前查 `node_modules/@arco-design/web-vue/es/<component>/` 源码或实测，不套用 React 版文档。典型反例：`a-progress` 的 `percent` 是 **0–1** 比例（`es/progress/line.js` 内 `NP.times(percent, 100)` 生成文本、`percent * 100%` 生成条宽），后端出参已是 0–1 时页面**不得再乘 100**（曾导致库存占比显示 `9920%`）。
- 需要固定显示格式时用组件插槽自行格式化（如 `a-progress` 的 `#text`），不依赖组件默认文案拼接的小数位。
- 升级 `@arco-design/web-vue` 时必须复核本条语义，并把口径变化同步到使用处与对应规格 §0。

## 5. 列表页约定

> **正文（页面布局、表格配置、列宽策略、交互模式）见 `specs/006-list-showcase/design.md` §0**；本节只留判据。参照实现：`src/views/Showcase/ListShowcaseView.vue`。

- 页面头**仅放标题**，不放任何创建按钮；操作按钮统一并入表格卡片上方的工具条（筛选行 + 操作行；主操作靠左，数据 / 视图操作靠右，组间竖 `a-divider`）。
- 表格必设 `row-key`；分页用 `showTotal` + `showPageSize`（`pageSizeOptions` 取值见 §0）；枚举字段用 `a-tag` 着色展示。
- **每列必设 `width`**（不留无宽度弹性列）；长文本列 `ellipsis` + `tooltip`；表格横向滚动按各列 `width` 求和，不写死。操作列宽度按 §5.2。
- 筛选条件「输入态 / 已应用态」分离（点「搜索」或回车才应用并回第 1 页）；列表请求必须用**请求序号仲裁**乱序响应，禁止用 `if (loading) return` 丢弃点击。
- 删除（单行 / 批量）一律 `a-popconfirm`；导出 CSV 加 `\uFEFF` BOM 头（Excel 中文兼容）。

### 5.2 操作列约定（强制）

> 适用范围：所有表格类列表页的操作列；工具条（页面级操作行）不适用。
> **正文（数量阈值与列宽参考、颜色映射表、图标映射表、密度样式、二次确认、固定显示、按钮顺序）见 `specs/011-action-column/design.md` §0**；本节只留判据。

- 操作数 > 3 时保留前 3 个平铺，其余收纳进行内「更多」下拉；「更多」触发按钮恒在末尾。
- 行内按钮一律 `type="text" size="small"`；颜色按操作语义固定（映射表见 §0）——警示 / 危险 / 完成各用对应 `status`，撤销类用次要色（`.action-btn-secondary`，**不用** `type="secondary"`），其余默认。
- 一操作一图标（Tabler，`#icon` 插槽，按需具名导入，映射表见 §0）；平铺顺序固定为 主操作 → 中性 → 完成 → 警示 → 危险。
- 危险 / 警示操作必须 `a-popconfirm` 二次确认；收纳进「更多」的同样要确认，禁止无确认直接执行。
- 列宽按 §0 的列宽参考取值（随操作数与是否含「更多」而定），不自行估宽。
- 操作列固定显示、不参与「列设置」勾选；行内写操作 loading 按 §4.6（`xxingId`），收纳进「更多」的写操作同样按 `xxingId` 互斥（`a-doption :disabled`）。

## 5.5 表单页与详情页约定（列表的配套交互）

> 参照实现与**正文（形态选择表及字段数阈值、页面职责）**见 `specs/007-form-detail-showcase/design.md` §0；本节只留判据。

- 形态按 §0 的字段数阈值选择：字段少且单屏可容纳用抽屉，字段多 / 多分组 / 含子表格用独立页面；弹窗仅用于确认类轻交互（不承载业务表单）；详情页一律独立页面。
- **详情页与表单页不共享组件**（仅共享数据模型 TS 类型）；字段极少的简单实体例外：详情复用表单的 disabled 态。
- 新增 / 编辑**共用**同一表单，以 `mode: 'create' | 'edit'` 区分，编辑时深拷贝回填（不污染数据源）。
- 校验用 `a-form :rules` + `formRef.validate()`，失败保持页面 / 抽屉不关闭；独立表单页提交成功跳详情页并 `Message.success`，抽屉提交成功关闭并通知列表刷新。
- 详情 id 不存在显示 `a-result status="404"` + 返回列表按钮，禁止白屏。

## 6. 状态与路由

状态用 Pinia（按功能拆分 `stores/<feature>.ts`）、路由用 `vue-router`（在 `router/` 统一注册，命名与功能域一致）、路由级代码分割用 `() => import(...)` 懒加载；文件位置见 `.codebuddy/CONTEXT.md` §3。

## 7. 编码约定

- 格式与命名由 ESLint（`npm run lint`）统一约束：2 空格缩进、变量 / 函数 camelCase、类型 / 接口 / 组件 PascalCase、常量 UPPER_SNAKE_CASE、禁 `any`（类型不确定用 `unknown` 收窄）；当前**不并行启用 Prettier**（无配置与脚本；启用须同时补 `.prettierrc`、`format` 脚本与 `eslint-config-prettier`）。
- 异步逻辑用 `async/await`，必须处理失败分支（配合接口层统一错误提示）。
- `utils/` 只放无副作用的纯函数（按主题一个文件，如 `datetime.ts`），不放组件状态、请求与业务编排。

## 8. 认证（JWT）

- token 存储、携带与 40100 处置的全局约定见 `AGENTS.md` §4.6；本处为实现细节：
  - 请求拦截器：从 `localStorage` 读取 token，附加 `Authorization: Bearer <token>`。
  - 响应拦截器：收到 `code: 40100` 时清除 `localStorage` 凭证，并经**路由层注入的处置回调**跳转登录页（`router.replace` + `redirect` 回跳目标，**不整页刷新**，防重复跳转）；跳转前必须清空 Pinia 认证状态，否则守卫读到的 `isLoggedIn` 仍为真，会把 `/login` 重定向回首页。请求层不得直接依赖 router / store（`stores/auth` 已依赖 `api/request`，反向依赖成环），统一用 `setUnauthorizedHandler` 注入处置逻辑。

## 9. 环境与联调

- 环境划分与配置原则见 `AGENTS.md` §7（仅 dev / prod）；实现上使用 Vite 多环境文件 `.env.development` / `.env.production`。
- API 基础地址通过 `import.meta.env.VITE_API_BASE_URL` 读取；dev 环境指向本地 / dev 后端（可用 Vite proxy）。
- 接口行为以联调结果为准。

## 10. 测试（e2e）

- e2e 测试策略与强制门槛见 `AGENTS.md` §6；此处只列本仓库的运行细则。
- 用例与配置：`frontend/e2e/*.spec.ts` + `playwright.config.ts`。
- 命名：用例文件为 `e2e/<功能域>.spec.ts`，域名取**功能语义短名**、kebab-case，与 `specs/<序号>-<feature>/`、路由同域（§4.1）——**不抄 `views/` 目录名、不带 `-management` 后缀**（`PurchaseManagement/` → `purchase.spec.ts`，`InventoryManagement/` → `inventory.spec.ts`）；一个功能域可有多个文件（`purchase.spec.ts` + `purchase-order.spec.ts`）。既有文件已按此收敛，**新增一律按此命名，不得新增例外**。
- 运行：需先起后端 dev（5080）与前端 dev（5173）再跑 `npm run test:e2e`（命令与端口见 `.codebuddy/CONTEXT.md` §4）；冷启动预热由 `e2e/global-setup.ts` 自动完成，无需手工预热。
- **数据隔离（推荐）**：跑全量用 `npm run e2e:run`（自动完成「清残留库 → 起后端连本轮 `app_e2e_<时间戳>` 库 → 跑用例 → 停后端 → 删库」，每轮全新、零残留；编排见 `specs/002-frontend-e2e/design.md` §6）；调试单用例仍可手工起服务 + `npm run test:e2e`（此时数据落在开发库）。

### 10.1 e2e 稳定性（强制）

- **禁止在加载态中点击**：Arco Button 在 `loading` 期间会**吞掉 click**（不报错、无副作用），用例若在页面首屏请求完成前点「搜索」，表现为筛选未应用，极易被误判为功能 bug / 后端未过滤。统一改用 helpers 的重试点击：`clickUntil`（`e2e/helpers/action.ts`，点按钮直到判据成立）、`searchAndWaitHit`（`e2e/helpers/table-search.ts`，关键字搜索直到命中行出现且未命中行归零）；不要手写「点一次 + 断言」。
- **重试判据必须取用例自身的期望**：判据可以是命中行 / 空状态（`.arco-empty`）/ 精确行数 / URL / 表头列等，**不可一律写成「命中关键字」**——空结果用例、按分类聚合（行内不含商品编码）的报表页，该判据与用例语义相反、必然失败。行数类判据用 `clickUntilCount`，URL 等无法用可见性表达的判据给 `clickUntil` 传断言块。
- **断言必须落在可见结果上**：筛选类用例除校验请求参数外，必须断言过滤**确实生效**（行数 / 内容符合筛选条件），否则「点击被吞、筛选未应用」会被静默漏过。
- **下拉选项的等待 / 点击须限定可见**：Arco Select 选中后旧弹层 DOM 会残留且处于隐藏态，`page.locator('.arco-select-option').first()` 会命中隐藏的旧浮层（表现为 `toBeVisible` 始终 hidden、或 click 卡在 actionability），须用 `.arco-select-option:visible` 限定当前浮层。
- **消息断言统一走 helper**：Arco Message 同文案在展示期内（默认 3s）连续弹出会堆叠（旧节点未销毁），`page.getByText('xx已创建')` 会命中多个元素并触发 strict mode violation（表现为「连续创建两个同类实体」的用例偶发失败）。一律用 `e2e/helpers/message.ts` 的 `expectMessage(page, text)` / `messageLocator(page, text)`，**不要**手写 `getByText` 消息断言，`clickUntil` 的判据 locator 同样适用。
- **登录统一走 `loginAs`（`e2e/helpers/auth.ts`）**，两条硬性约束：
  - 清凭证必须限定在「导航目标是 `/login`」时执行。init script 对该 page 的**每次完整导航**都会跑，无条件清 token 会让「登录后用 `page.goto('/xxx')` 直达页面」的用例被路由守卫重定向回登录页（表现为菜单 / 页面内容缺失）。
  - 清凭证前先离开业务页（`about:blank`）。init script 会立即在当前页面执行一次，业务页在途请求会因无 token 返回 40100，触发 40100 统一处置（跳登录页，见 §8），与随后的 `goto('/login')` 相互干扰、导航交叉（表现为登录表单不渲染、`fill` 等到用例超时）；先离开业务页可避免。
  - 登录前等登录表单可见，未渲染则重新导航重试，不在空白页上直接 `fill`。
- 用例失败时先排查上述时序问题，再怀疑业务实现；排查依据以 `test-results/<用例名>/error-context.md`（失败时由 Playwright 生成，含页面实际内容）为准，不靠猜测。

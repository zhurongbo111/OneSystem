---
description: 前端实现规则。开发或修改 frontend/ 下的 Vue 3 / Arco Design / TypeScript 代码时应用，含技术栈、目录结构、组件与状态管理约定。
alwaysApply: false
enabled: true
---

# 前端实现规则

> 总则见根目录 `AGENTS.md`（SDD 工作流、API 契约、错误码、通用约定与测试门槛）。本文件只写前端专属实现约定，总则已规定的条款不再重复列出；确需引用时标注章节（如 `AGENTS.md` §4.5 / §6）。
> 开发功能前：先读 `specs/<feature>/` 规格（`AGENTS.md` §2.2），再按本文件实现。

## 1. 技术栈

- Vue 3 + TypeScript（`<script setup lang="ts">` 组合式 API，禁止 Options API）
- Arco Design Vue（UI 组件库）
- Vite（构建）、Pinia（状态）、vue-router（路由）、Axios（HTTP）

## 2. 目录结构

```
frontend/
├── index.html
├── vite.config.ts
├── package.json
└── src/
    ├── api/         # 接口请求层（统一响应解包、类型定义）
    ├── assets/
    ├── components/  # 通用组件
    ├── composables/ # 组合式函数
    ├── router/      # 路由
    ├── stores/      # Pinia 状态
    ├── utils/
    └── views/       # 页面级组件
```

## 3. 接口层

- 所有接口请求集中在 `src/api/`，按业务域拆分文件（如 `user.ts`）。
- 统一封装：基于 Axios 拦截器解包 `{ code, message, data }`；`code !== 0` 时用 Arco `Message.error` 提示 `message` 并 reject。
- 请求参数与响应定义 TS 类型，与后端 DTO（camelCase）一一对应。
- 组件内禁止直接调用 `axios/fetch`，必须经 `src/api/` 模块。

## 4. 组件与页面

- 页面级组件放 `src/views/`，按功能目录组织；可复用组件放 `src/components/`。
- UI 统一使用 Arco Design 组件，不自造样式体系；自定义样式优先使用 Arco 主题变量（`@arco-design` CSS 变量 / 主题定制）。
- 组件 props / emits 必须声明 TS 类型。
- 组件名 PascalCase，文件命名与组件名一致。

### 4.1 页面命名与目录归属（强制）

- `src/views/` 按**功能域**分目录，一个域一个 `<Domain>/`；域的划分须与后端 `Features/<Feature>`、路由前缀、侧边菜单项、e2e spec **四者对齐**（如 `CategoryManagement/` ↔ `Features/Categories` ↔ `/categories` ↔ `category-management.spec.ts`）。后端 / 规格已独立的域，前端不得挂在别的域目录下。
- 域目录内文件一律**平铺**，不再套 `components/` 子目录；被多个页面共用的私有组件同样平铺在该域目录内，只有跨功能域复用的通用组件才进 `src/components/`。
- 命名单复数：列表页用**复数域名**（`ProductsView` / `PurchasesView` / `SalesView`），表单页与详情页用**单数实体名**（`ProductFormDrawer` / `PurchaseFormPage` / `SaleDetailView` / `UserDetailView`），与 `src/api/<entity>.ts` 一致；集合概念名词（`Inventory`）保留单数。
- 无功能域归属的独立页（`LoginView` / `HomeView`）平铺在 `views/` 根；演示 / 示例页统一收进 `views/Showcase/`，不与业务页混层。

### 4.5 组合式 API 分区书写规范（强制）

> 规格：`specs/composable-style/`。目的：禁止随意位置写 `ref` / `computed` / `watch` / 函数，保证结构可预期。

**`<script setup>` 固定分区顺序**（自上而下，分区间空行分隔；只允许缺省，不允许乱序或穿插）：

| # | 分区 | 内容 |
|---:|---|---|
| 1 | imports | 全部 `import`（vue 生态 → 第三方 → 本地 `@/`，`type` 导入用 `type` 关键字） |
| 2 | types | 组件局部 `interface` / `type` |
| 3 | props/emits | `defineProps` / `defineEmits` / `defineOptions`（仅组件） |
| 4 | constants | 选项数组、映射表、配置对象、种子数据等纯 `const` |
| 5 | helpers | 私有纯函数（不依赖响应式），如 `cloneData` / `formatDate` / `emptyForm` |
| 6 | stores/composables | `useRouter` / `useRoute` / `useXxxStore()` / `useXxx()` 调用 |
| 7 | reactive state | `ref` / `reactive` / `shallowRef`（含模板 ref） |
| 8 | computed | `computed` 派生状态 |
| 9 | watch | `watch` / `watchEffect` |
| 10 | lifecycle | `onMounted` / `onUnmounted` 等 |
| 11 | methods | 业务函数（事件处理、提交、删除等） |

- 同一分区内「被引用者在前」：`computed` 依赖的 `ref` 在前、派生 `computed` 在后。
- 分区注释：`// —— reactive state ——` 形式；分区内声明 > 1 个或为 methods 区时必须写，单声明小分区可省略。
- **模板 ref 命名强制 `xxxRef`**（如 `formRef`），与 `eslint-plugin-perfectionist` 规则一致。
- **composable（`composables/*.ts`）**：`imports → types → constants（含模块级单例 ref，需注释）→ helpers → export function useXxx()`；函数体内同样遵守 reactive state → computed → watch → methods 相对顺序。
- lint 强制项（import 排序、types 先于变量、ref 命名）见 `eslint.config.js`；分区顺序以本表为准 + code review 把关。

### 4.6 按钮交互反馈（强制）

> 目的：任何会发起请求的按钮，点击后必须**立即**给出 loading 反馈，且操作进行中不可重复触发。

**绑定粒度：loading 属于「操作」而非「按钮」**。同一个异步操作无论有几个触发入口（按钮 / 输入框回车 / 分页器 / 行内按钮），共用同一个 loading 状态；不同操作各用独立状态，**禁止多个按钮共用一个 `loading`**。

| 操作类型 | 状态命名 | 绑定方式 |
|---|---|---|
| 列表查询（搜索 / 重置 / 刷新 / 翻页 / 每页条数） | `loading` | 按钮 + 回车 `:loading="loading"`，同时 `<a-table :loading="loading">` |
| 表单提交（新增 / 编辑） | `submitting` | 提交按钮 `:loading="submitting"` |
| 弹窗确认（重置密码 / 改状态等） | `xxSubmitting` | `a-modal :ok-loading="xxSubmitting"` |
| 行内操作（启用 / 禁用 / 单行删除） | `xxingId`（如 `togglingId`） | `:loading="togglingId === row.id"` |

**实现要求**

- **触发即置位**：handler 内**先** `xxLoading.value = true`，**再** `await`；用 `try/finally` 在 `finally` 中复位，成功与失败路径都要复位，禁止只在成功分支复位。
- **防重入**：动作类（提交 / 删除 / 启停）handler 开头必须 `if (xxLoading.value) return`。Arco 的 `loading` 只表现为不可点击，**不阻止事件重入**（如回车 + 点击、弹窗确认连点），需显式判空。
- **查询类不丢弃点击**：搜索 / 翻页等查询动作，最新意图必须生效，**不要**加 `if (loading.value) return`；改为用请求序号仲裁乱序响应（见 §5 交互模式）。
- **保留业务 `:disabled`**：与 loading 可叠加，如 `:disabled="!selectedIds.length"` + `:loading="loading"`；`a-button` 的 `loading` 已隐含不可点击，无需为它另写 `:disabled`。
- **不置 loading 的情形**：同步瞬时动作（打开抽屉 / 弹窗、路由跳转、本地导出 CSV、复制文本）不置 loading；只有「发起请求并等待结果」才置。
- **行内操作只转被点行**：用「当前操作行 id」判断，避免整列按钮同时转圈。

**e2e**：按钮 loading 属用户可感知行为，相关功能交付前须有 e2e 断言（`AGENTS.md` §6）。

### 4.7 图标选用（强制）

> 项目并存三套图标库：Tabler（`@tabler/icons-vue`，业务默认）、Lucide（`@lucide/vue`）、Arco Design 自带图标（`@arco-design/web-vue` 的 `Icon*`）。三套的用法与示例见组件示例页「图标」tab（`/components`，规格 `specs/icon-showcase/`）。

**选型优先级（从高到低，逐级回退，命中即用，禁止反向选择）**

| 优先级 | 类库 | 使用场景 |
|---:|---|---|
| 1 | Tabler | 首选，图标量大、业务语义覆盖全（单据、往来单位、仓储、结算等） |
| 2 | Lucide | Tabler 无对应语义图标时 |
| 3 | Arco 自带 `Icon*` | 前两者均无对应语义图标时兜底（Arco 组件自身内置的图标形态除外） |

- 按**业务语义**查找（如「重置」→ `IconRestore`），不是按组件名猜。
- **禁止反向选择**：不得因偏好某套风格跳过 Tabler 直接用 Lucide / Arco；不得为凑图标使用语义不符的图标（宁缺毋滥，纯装饰性图标可放宽）。
- **Arco 组件 `#icon` 插槽**（`a-button` / `a-menu-item` / `a-select` 等，含 §5.2 操作列固定图标）**同样优先 Tabler**，三套均可放进插槽；仅当语义只有 Arco 覆盖时才用 Arco。
- **来源注释**：使用 Lucide / Arco 图标时，在 import 处加一行注释说明，如 `// 图标来源：Lucide（Tabler 无对应语义）`，便于 review。
- **尺寸**：Tabler / Lucide 默认 `24px`，全局样式（`src/App.vue`）已统一收敛：Arco 按钮 / 下拉项内为 `1em`（与 Arco 自带图标同视觉重量），侧边菜单为辅以 `AppLayout` scoped 规则的 `18px` + `stroke-width 2.5`；其余场景在空间不足时再按需用 `:size`，避免逐处写。
- **线宽**：Tabler / Lucide 默认 `stroke 2`，Arco 默认 `stroke-width 4`；同一区域（如侧边菜单）内不混用不同来源，避免线宽视觉参差；小尺寸（≤ 18px）场景可将 Tabler 提到 `2.5` 补清晰度。
- **迁移状态**：侧边菜单、列表工具条与操作列的业务图标已完成 Tabler 迁移，仅「图标」示例页（`/components` 图标 tab，规格 `specs/icon-showcase/`）为演示保留三套并存。新增 / 修改图标一律按本表选 Tabler，且同一业务图标全项目统一用同一来源。

## 5. 列表页约定

- 所有表格类列表页以 `src/views/ListShowcaseView.vue` 为标准参照实现（规格 `specs/list-showcase/`）；新增列表页优先复制其结构与交互模式再替换业务字段。

**页面布局**
- 页面头仅放标题；操作按钮不放页面头，统一并入表格卡片上方的工具条（避免操作离列表太远）。
- 工具条分两行，整体与表格同在一个无描边 `a-card` 内，紧贴表格：
  - 筛选行：`a-row`（`wrap`）+ `a-col` 栅格排多列，窄屏自动换行为多行；末列放「搜索」（primary，`<IconSearch />`）+「重置」（`<IconRestore />`）按钮，图标走 `#icon` 插槽（重置用 `IconRestore`（恢复初始态语义），不复用已归属「刷新」的 `IconRefresh`）；行底用边框线与操作行分隔。
  - 操作行：紧贴表格，左右分组（`justify-content: space-between`）；**主操作（新增/开单）统一靠左**，其余操作（数据操作 导出、批量删除 / 视图操作 列设置、刷新）靠右且与主操作同一行，组间用竖 `a-divider` 分隔；按钮统一 `size="small"`；无主操作的只读页（库存、登录日志）整行右对齐即可。页面头不放任何创建按钮（采购/销售开单入口同样在操作行左组）。
- 表格配置：`row-key` 必设；分页 `showTotal` + `showPageSize`（`pageSizeOptions: [10, 20, 50]`）；枚举字段（角色/状态等）用 `a-tag` 着色展示；操作列放表格末列，`a-button type="text"`，呈现细则见 §5.2 操作列约定。
- 列宽策略：每列都设 `width`，不设无宽度弹性列（唯一无宽度列会吸收全部剩余空间，宽屏下被撑到 400px+）；长文本列固定 `width` + `ellipsis: true, tooltip: true`；表格设 `:scroll="{ x: tableScrollX }"`（`tableScrollX` = 各列 `width` 之和）——总和小于容器时剩余空间按列宽比例分摊，窄屏横向滚动。操作列宽度按 §5.2 列宽参考取值。

### 5.2 操作列约定（强制）

> 规格：`specs/action-column/`。适用范围：所有表格类列表页的操作列；工具条（页面级操作行）不适用，其分组见本节「页面布局」。

**数量：超过 3 个操作收纳进「更多」下拉**

| 操作数 | 呈现 |
|---|---|
| ≤ 3 | 全部平铺（`a-space :size="4"`） |
| > 3 | 保留前 3 个平铺，其余收纳进行内「更多」下拉（`a-dropdown trigger="click"` + `a-doption`，触发按钮 `type="text" size="small"` + `IconDotsVertical`） |

- 平铺选取：常用 / 主操作 + 危险操作优先平铺，低频操作进「更多」；「更多」触发按钮恒在末尾。
- 列宽参考：平铺 ≤ 3 个短操作 `150~220`；含「更多」时 `200~260`。
- 平铺按钮顺序：主操作 → 中性 → 警示 → 危险。

**颜色：按操作类型固定（行内按钮一律 `type="text" size="small"`）**

| 语义 | 按钮属性 | 适用操作 |
|---|---|---|
| 主操作 | 默认 | 编辑 |
| 警示 | `status="warning"` | 禁用 / 停用 |
| 危险 | `status="danger"` | 删除 / 移除（不可逆） |
| 完成 | `status="success"` | 标记已收 / 已付（结算达成，与绿标签同色） |
| 中性 | 默认 | 详情、查看、重置密码、启用 / 重新启用 |
| 次要 | `.action-btn-secondary`（`var(--color-text-2)`） | 改回未收 / 未付（反向撤销，视觉降级） |

- 启用是恢复操作，不用 success 色；「改回未收 / 未付」降为**次要色**（次级文字灰），既不与「详情」同色，也不突出。结算切换两方向靠颜色（success / 次要）+ 图标（`IconCircleCheck` / `IconArrowBackUp`）+ 文案区分。
- 次要色用 scoped 样式覆盖 `.arco-btn-text` 颜色实现，**不用** `type="secondary"`（灰底实色按钮，在操作列里带背景块、视觉重量过大）。
- 危险 / 警示操作必须 `a-popconfirm` 二次确认。
- 「更多」菜单项不染色（Arco 菜单项无 `status` 语义位），危险语义由图标 + 二次确认承担；收纳进「更多」的危险 / 警示操作 `@click` 打开确认，禁止无确认直接执行。

**图标：一操作一图标（Tabler 图标，`#icon` 插槽，按需具名导入）**

| 操作 | 图标 | 操作 | 图标 |
|---|---|---|---|
| 编辑 | `IconEdit` | 重置密码 | `IconLock` |
| 详情 / 查看 | `IconEye` | 导出 | `IconDownload` |
| 删除 | `IconTrash` | 更多（收纳触发） | `IconDotsVertical` |
| 启用（恢复） | `IconPlayerPlay` | 禁用 / 停用 | `IconPower` |
| 作废（单据作废回冲） | `IconBan` | 结算切换：标记已收 / 已付 | `IconCircleCheck` |
| | | 结算切换：改回未收 / 未付 | `IconArrowBackUp` |

- 表外操作选 Tabler 语义最近图标，且同一图标全项目只对应一个操作。

**密度**：Arco 文本按钮默认 `padding: 0 15px`，操作列内过宽。操作列按钮（含纯图标「更多」触发按钮）统一 `padding: 0 8px`，列表页 scoped 样式实现（`a-space` 加 `class="row-actions"`）：

```css
.row-actions :deep(.arco-btn-text),
.row-actions :deep(.arco-btn-only-icon) {
  padding: 0 8px;
}
```

**其他**：操作列固定显示，不参与「列设置」勾选；行内写操作 loading 按 §4.6（`xxingId`），收纳进「更多」的写操作在确认入口同样按 `xxingId` 互斥（`a-doption :disabled`）。

**交互模式**
- 筛选条件「输入态 / 已应用态」分离：只有点「搜索」（或回车）才写入已应用态，条件变化后清空勾选。
- 条件变化后回到第 1 页：已应用条件拼成字符串作 `:key` 重挂载表格。
- 查询动作（搜索 / 重置 / 刷新 / 翻页 / 每页条数）并发不可避免（HTTP 不串行、事件回调不阻塞、`void fetchList()` 不等待），列表请求必须用请求序号仲裁：`const seq = ++fetchSeq` 记序号，响应回来时 `seq !== fetchSeq` 则丢弃，`loading` 也只在最后发起的请求上复位；禁止用 `if (loading) return` 丢弃新点击。
- 查询类按钮的 loading 绑定见 §4.6（与表格共用同一个 `loading`）。
- 删除（单行、批量）一律 `a-popconfirm` 确认；批量删除按钮在无勾选时 `:disabled`。
- 导出当前筛选结果为 CSV 时加 `\uFEFF` BOM 头（Excel 中文兼容）。

## 5.5 表单页与详情页约定（列表的配套交互）

> 参照实现见 `specs/form-detail-showcase/`（统一列表 `/form` + 抽屉形态 / 独立页面形态）。

**形态选择**

| 场景 | 表单形态 |
|---|---|
| 字段少（≤ 8 个）、单屏可容纳 | **抽屉**（`a-drawer` 宽 500~720，`unmount-on-close`，`:footer="false"` + 底部自定义操作栏，手动 `validate()` 后提交） |
| 字段多（> 8 个）/ 多分组 / 含子表格 | **独立页面**（路由 `new` / `edit/:id`，底部操作栏 提交/取消） |
| 弹窗（Modal） | 仅用于确认类轻交互（改状态、单字段修改），不承载业务表单 |
| 详情页 | 一律**独立页面**（`/xxx/detail/:id`），`a-page-header`（`@back` 回列表）+ `a-descriptions` |
| 同一列表需同时提供两种形态 | 新增：工具条两个按钮（primary「新增·抽屉」+ 次级「新增·页面」）；编辑：操作列单按钮 + `a-dropdown` 形态菜单（用抽屉 / 用页面） |

**页面职责（强制）**

- **详情页与表单页不共享同一组件**：详情页只读、强调信息密度（可展示创建人/更新时间等表单没有的只读字段）；表单页强调录入与校验。两者仅共享同一份数据模型 TS 类型。字段极少（< 5 个）的简单实体例外：详情页可复用表单组件的 disabled 态。
- 新增/编辑**共用**同一个表单（抽屉或页面），以 `mode: 'create' | 'edit'` 区分，编辑时深拷贝回填（不污染数据源）。
- 表单必填校验用 `a-form :rules` + `formRef.validate()`；校验失败保持页面/抽屉不关闭。
- 独立表单页提交成功跳对应详情页并 `Message.success`；抽屉提交成功后关闭并通知列表刷新。
- 详情 id 不存在：显示 `a-result status="404"` + 返回列表按钮，禁止白屏。
- 详情/表单跨路由共享会话内数据时，用 `composables/` 模块级单例（不为此引入 Pinia）；接真实接口后以接口为事实源。

## 6. 状态与路由

- 状态管理使用 Pinia，按功能拆分 store（`stores/<feature>.ts`）。
- 路由使用 `vue-router`，路由按功能拆分并在 `router/` 统一注册；路由命名与功能域一致。
- 路由级代码分割使用 `() => import(...)` 懒加载。

## 7. 编码约定

- 2 空格缩进；ESLint + Prettier 保持统一格式。
- 变量 / 函数 camelCase，类型 / 接口 / 组件 PascalCase，常量 UPPER_SNAKE_CASE。
- 禁止 `any`；无法确定类型时使用 `unknown` 收窄。
- 异步逻辑用 `async/await`，必须处理失败分支（配合接口层统一错误提示）。

## 8. 认证（JWT）

- token 存储、携带与 40100 处置的全局约定见 `AGENTS.md` §4.6；本处为实现细节：
  - 请求拦截器：从 `localStorage` 读取 token，附加 `Authorization: Bearer <token>`。
  - 响应拦截器：收到 `code: 40100` 时清除 `localStorage` 凭证并跳转登录页（防重复跳转）。

## 9. 环境与联调

- 环境划分与配置原则见 `AGENTS.md` §7（仅 dev / prod）；实现上使用 Vite 多环境文件 `.env.development` / `.env.production`。
- API 基础地址通过 `import.meta.env.VITE_API_BASE_URL` 读取；dev 环境指向本地 / dev 后端（可用 Vite proxy）。
- 接口行为以联调结果为准。

## 10. 测试（e2e）

- e2e 测试策略与强制门槛见 `AGENTS.md` §6；此处只列本仓库的运行细则。
- 用例与配置：`frontend/e2e/*.spec.ts` + `playwright.config.ts`。
- 运行：先启动后端 dev（`cd backend && dotnet run --project src/App.Api`，端口 5080）与前端 dev（`cd frontend && npm run dev`，端口 5173），再执行 `cd frontend && npm run test:e2e`。

---
description: 前端实现规则。开发或修改 frontend/ 下的 Vue 3 / Arco Design / TypeScript 代码时应用，含技术栈、目录结构、组件与状态管理约定。
alwaysApply: false
enabled: true
---

# 前端实现规则

> 总则见根目录 `AGENTS.md`（SDD 工作流、API 契约、错误码、通用约定与测试门槛）。本文件只写前端专属实现约定，总则已规定的条款不再重复列出；确需引用时标注章节（如 `AGENTS.md` §4.5 / §6）。
> 开发功能前：先读 `specs/<feature>/` 规格（`AGENTS.md` §2.2），再按本文件实现。

## 1. 技术栈

- Vue 3 + TypeScript（`<script setup lang="ts">` 组合式 API，禁止 Options API）
- Arco Design Vue（UI 组件库）
- Vite（构建）、Pinia（状态）、vue-router（路由）、Axios（HTTP）

## 2. 目录结构

```
frontend/
├── index.html
├── vite.config.ts
├── package.json
└── src/
    ├── api/         # 接口请求层（统一响应解包、类型定义）
    ├── assets/
    ├── components/  # 通用组件
    ├── composables/ # 组合式函数
    ├── router/      # 路由
    ├── stores/      # Pinia 状态
    ├── utils/
    └── views/       # 页面级组件
```

## 3. 接口层

- 所有接口请求集中在 `src/api/`，按业务域拆分文件（如 `user.ts`）。
- 统一封装：基于 Axios 拦截器解包 `{ code, message, data }`；`code !== 0` 时用 Arco `Message.error` 提示 `message` 并 reject。
- 请求参数与响应定义 TS 类型，与后端 DTO（camelCase）一一对应。
- 组件内禁止直接调用 `axios/fetch`，必须经 `src/api/` 模块。

## 4. 组件与页面

- 页面级组件放 `src/views/`，按功能目录组织；可复用组件放 `src/components/`。
- UI 统一使用 Arco Design 组件，不自造样式体系；自定义样式优先使用 Arco 主题变量（`@arco-design` CSS 变量 / 主题定制）。
- 组件 props / emits 必须声明 TS 类型。
- 组件名 PascalCase，文件命名与组件名一致。

### 4.5 组合式 API 分区书写规范（强制）

> 规格：`specs/composable-style/`。目的：禁止随意位置写 `ref` / `computed` / `watch` / 函数，保证结构可预期。

**`<script setup>` 固定分区顺序**（自上而下，分区间空行分隔；只允许缺省，不允许乱序或穿插）：

| # | 分区 | 内容 |
|---:|---|---|
| 1 | imports | 全部 `import`（vue 生态 → 第三方 → 本地 `@/`，`type` 导入用 `type` 关键字） |
| 2 | types | 组件局部 `interface` / `type` |
| 3 | props/emits | `defineProps` / `defineEmits` / `defineOptions`（仅组件） |
| 4 | constants | 选项数组、映射表、配置对象、种子数据等纯 `const` |
| 5 | helpers | 私有纯函数（不依赖响应式），如 `cloneData` / `formatDate` / `emptyForm` |
| 6 | stores/composables | `useRouter` / `useRoute` / `useXxxStore()` / `useXxx()` 调用 |
| 7 | reactive state | `ref` / `reactive` / `shallowRef`（含模板 ref） |
| 8 | computed | `computed` 派生状态 |
| 9 | watch | `watch` / `watchEffect` |
| 10 | lifecycle | `onMounted` / `onUnmounted` 等 |
| 11 | methods | 业务函数（事件处理、提交、删除等） |

- 同一分区内「被引用者在前」：`computed` 依赖的 `ref` 在前、派生 `computed` 在后。
- 分区注释：`// —— reactive state ——` 形式；分区内声明 > 1 个或为 methods 区时必须写，单声明小分区可省略。
- **模板 ref 命名强制 `xxxRef`**（如 `formRef`），与 `eslint-plugin-perfectionist` 规则一致。
- **composable（`composables/*.ts`）**：`imports → types → constants（含模块级单例 ref，需注释）→ helpers → export function useXxx()`；函数体内同样遵守 reactive state → computed → watch → methods 相对顺序。
- lint 强制项（import 排序、types 先于变量、ref 命名）见 `eslint.config.js`；分区顺序以本表为准 + code review 把关。

## 5. 列表页约定

- 所有表格类列表页以 `src/views/ListShowcaseView.vue` 为标准参照实现（规格 `specs/list-showcase/`）；新增列表页优先复制其结构与交互模式再替换业务字段。

**页面布局**
- 页面头仅放标题；操作按钮不放页面头，统一并入表格卡片上方的工具条（避免操作离列表太远）。
- 工具条分两行，整体与表格同在一个无描边 `a-card` 内，紧贴表格：
  - 筛选行：`a-row`（`wrap`）+ `a-col` 栅格排多列，窄屏自动换行为多行；末列放「搜索」（primary）+「重置」按钮；行底用边框线与操作行分隔。
  - 操作行：右对齐、紧贴表格；按钮统一 `size="small"`；按 主操作（新增）/ 数据操作（导出、批量删除）/ 视图操作（列设置、刷新）分组，组间用竖 `a-divider` 分隔。
- 表格配置：`row-key` 必设；分页 `showTotal` + `showPageSize`（`pageSizeOptions: [10, 20, 50]`）；枚举字段（角色/状态等）用 `a-tag` 着色展示；操作列放表格末列，`a-button type="text"`。

**交互模式**
- 筛选条件「输入态 / 已应用态」分离：只有点「搜索」（或回车）才写入已应用态，条件变化后清空勾选。
- 条件变化后回到第 1 页：已应用条件拼成字符串作 `:key` 重挂载表格。
- 删除（单行、批量）一律 `a-popconfirm` 确认；批量删除按钮在无勾选时 `:disabled`。
- 导出当前筛选结果为 CSV 时加 `\uFEFF` BOM 头（Excel 中文兼容）。

## 5.5 表单页与详情页约定（列表的配套交互）

> 参照实现见 `specs/form-detail-showcase/`（统一列表 `/form` + 抽屉形态 / 独立页面形态）。

**形态选择**

| 场景 | 表单形态 |
|---|---|
| 字段少（≤ 8 个）、单屏可容纳 | **抽屉**（`a-drawer` 宽 500~720，`unmount-on-close`，`:footer="false"` + 底部自定义操作栏，手动 `validate()` 后提交） |
| 字段多（> 8 个）/ 多分组 / 含子表格 | **独立页面**（路由 `new` / `edit/:id`，底部操作栏 提交/取消） |
| 弹窗（Modal） | 仅用于确认类轻交互（改状态、单字段修改），不承载业务表单 |
| 详情页 | 一律**独立页面**（`/xxx/detail/:id`），`a-page-header`（`@back` 回列表）+ `a-descriptions` |
| 同一列表需同时提供两种形态 | 新增：工具条两个按钮（primary「新增·抽屉」+ 次级「新增·页面」）；编辑：操作列单按钮 + `a-dropdown` 形态菜单（用抽屉 / 用页面） |

**页面职责（强制）**

- **详情页与表单页不共享同一组件**：详情页只读、强调信息密度（可展示创建人/更新时间等表单没有的只读字段）；表单页强调录入与校验。两者仅共享同一份数据模型 TS 类型。字段极少（< 5 个）的简单实体例外：详情页可复用表单组件的 disabled 态。
- 新增/编辑**共用**同一个表单（抽屉或页面），以 `mode: 'create' | 'edit'` 区分，编辑时深拷贝回填（不污染数据源）。
- 表单必填校验用 `a-form :rules` + `formRef.validate()`；校验失败保持页面/抽屉不关闭。
- 独立表单页提交成功跳对应详情页并 `Message.success`；抽屉提交成功后关闭并通知列表刷新。
- 详情 id 不存在：显示 `a-result status="404"` + 返回列表按钮，禁止白屏。
- 详情/表单跨路由共享会话内数据时，用 `composables/` 模块级单例（不为此引入 Pinia）；接真实接口后以接口为事实源。

## 6. 状态与路由

- 状态管理使用 Pinia，按功能拆分 store（`stores/<feature>.ts`）。
- 路由使用 `vue-router`，路由按功能拆分并在 `router/` 统一注册；路由命名与功能域一致。
- 路由级代码分割使用 `() => import(...)` 懒加载。

## 7. 编码约定

- 2 空格缩进；ESLint + Prettier 保持统一格式。
- 变量 / 函数 camelCase，类型 / 接口 / 组件 PascalCase，常量 UPPER_SNAKE_CASE。
- 禁止 `any`；无法确定类型时使用 `unknown` 收窄。
- 异步逻辑用 `async/await`，必须处理失败分支（配合接口层统一错误提示）。

## 8. 认证（JWT）

- token 存储、携带与 40100 处置的全局约定见 `AGENTS.md` §4.5；本处为实现细节：
  - 请求拦截器：从 `localStorage` 读取 token，附加 `Authorization: Bearer <token>`。
  - 响应拦截器：收到 `code: 40100` 时清除 `localStorage` 凭证并跳转登录页（防重复跳转）。

## 9. 环境与联调

- 环境划分与配置原则见 `AGENTS.md` §7（仅 dev / prod）；实现上使用 Vite 多环境文件 `.env.development` / `.env.production`。
- API 基础地址通过 `import.meta.env.VITE_API_BASE_URL` 读取；dev 环境指向本地 / dev 后端（可用 Vite proxy）。
- 接口行为以联调结果为准。

## 10. 测试（e2e）

- e2e 测试策略与强制门槛见 `AGENTS.md` §6；此处只列本仓库的运行细则。
- 用例与配置：`frontend/e2e/*.spec.ts` + `playwright.config.ts`。
- 运行：先启动后端 dev（`cd backend && dotnet run --project src/App.Api`，端口 5080）与前端 dev（`cd frontend && npm run dev`，端口 5173），再执行 `cd frontend && npm run test:e2e`。

---
description: 前端实现规则。开发或修改 frontend/ 下的 Vue 3 / Arco Design / TypeScript 代码时应用，含技术栈、目录结构、组件与状态管理约定。
alwaysApply: false
enabled: true
---

# 前端实现规则

> 总则（SDD 工作流、API 契约、通用约定）见根目录 `AGENTS.md`，本文件只写前端专属实现约定。
> 开发功能前：先读 `specs/<feature>/` 规格，再按本文件实现。

## 1. 技术栈

- Vue 3 + TypeScript（`<script setup lang="ts">` 组合式 API，禁止 Options API）
- Arco Design Vue（UI 组件库）
- Vite（构建）、Pinia（状态）、vue-router（路由）、Axios（HTTP）

## 2. 目录结构

```
frontend/
├── index.html
├── vite.config.ts
├── package.json
└── src/
    ├── api/         # 接口请求层（统一响应解包、类型定义）
    ├── assets/
    ├── components/  # 通用组件
    ├── composables/ # 组合式函数
    ├── router/      # 路由
    ├── stores/      # Pinia 状态
    ├── utils/
    └── views/       # 页面级组件
```

## 3. 接口层

- 所有接口请求集中在 `src/api/`，按业务域拆分文件（如 `user.ts`）。
- 统一封装：基于 Axios 拦截器解包 `{ code, message, data }`；`code !== 0` 时用 Arco `Message.error` 提示 `message` 并 reject。
- 请求参数与响应定义 TS 类型，与后端 DTO（camelCase）一一对应。
- 组件内禁止直接调用 `axios/fetch`，必须经 `src/api/` 模块。

## 4. 组件与页面

- 页面级组件放 `src/views/`，按功能目录组织；可复用组件放 `src/components/`。
- UI 统一使用 Arco Design 组件，不自造样式体系；自定义样式优先使用 Arco 主题变量（`@arco-design` CSS 变量 / 主题定制）。
- 组件 props / emits 必须声明 TS 类型。
- 组件名 PascalCase，文件命名与组件名一致。

## 5. 状态与路由

- 状态管理使用 Pinia，按功能拆分 store（`stores/<feature>.ts`）。
- 路由使用 `vue-router`，路由按功能拆分并在 `router/` 统一注册；路由命名与功能域一致。
- 路由级代码分割使用 `() => import(...)` 懒加载。

## 6. 编码约定

- 2 空格缩进；ESLint + Prettier 保持统一格式。
- 变量 / 函数 camelCase，类型 / 接口 / 组件 PascalCase，常量 UPPER_SNAKE_CASE。
- 禁止 `any`；无法确定类型时使用 `unknown` 收窄。
- 异步逻辑用 `async/await`，必须处理失败分支（配合接口层统一错误提示）。

## 7. 认证（JWT）

- token 统一存储在 `localStorage`；Axios 请求拦截器从 `localStorage` 读取并附加 `Authorization: Bearer <token>`。
- 响应拦截器：收到 `code: 40100` 时清除 `localStorage` 中的凭证并跳转登录页（需防重复跳转）。

## 8. 环境与联调

- Vite 多环境：`.env.development` / `.env.production`，目前仅 dev / prod 两个环境。
- API 基础地址通过 `import.meta.env` 读取（如 `VITE_API_BASE_URL`）；dev 环境指向本地 / dev 后端（可用 Vite proxy）。
- 集成测试：不单写单元测试；用 Playwright e2e 自动化（`frontend/e2e/*.spec.ts`，配置 `playwright.config.ts`），直连 dev 后端、不打 mock。
- **e2e 覆盖要求（强制）**：凡涉及前端改动的功能，交付前必须有对应 e2e 用例覆盖该改动的用户可感知行为（新增 / 变更的页面、交互、流程、异常分支）。无 e2e 覆盖的前端改动视为未完成，不得勾选规格 `tasks.md` 中对应任务。
- 运行 e2e 前置：先启动后端 dev（`cd backend && dotnet run --project src/App.Api`，端口 5080）与前端 dev（`cd frontend && npm run dev`，端口 5173），再执行 `cd frontend && npm run test:e2e`。
- **前后端改动须跑相关测试（强制）**：功能同时涉及前后端改动时，交付前必须两端都跑通——后端 `dotnet test`（单元测试）+ 前端 `npm run test:e2e`（集成测试）；只改一端时至少跑被改动端的测试。测试未通过不得声明功能完成。
- 接口行为以联调结果为准。

---
description: 后端实现规则。开发或修改 backend/ 下的 .NET 8 / ASP.NET Core / EF Core 代码时应用，含编码约定、分层架构、数据库规范、认证、日志、环境配置与测试约定。
alwaysApply: false
enabled: true
---

# 后端实现规则

> 总则见根目录 `AGENTS.md`（SDD 工作流、API 契约、错误码、通用约定与测试门槛）；本文件只写后端专属实现约定，总则已规定的条款不重复列出（确需引用时标注章节，如 `AGENTS.md` §4.5）。
> 开发功能前：先读 `specs/<序号>-<feature>/` 规格（`AGENTS.md` §2.2），再按本文件实现。

## 1. 技术栈

.NET 8 + ASP.NET Core Web API + EF Core（Npgsql / PostgreSQL）+ FluentValidation（校验器与用例同目录）。

## 2. 编码约定

> **C# 的格式与代码风格以 `backend/.editorconfig` 为唯一事实源**（缩进 4 空格 / CRLF / 大括号与换行、`using` 排序与分组、命名规则、IDE / CA severity）；本文件不重复其条目，调整风格只改 `.editorconfig`。
> 生效范围：`CA*`（代码质量）随 `dotnet build` 生效；`IDE*`（代码风格）**当前未随构建强制**（未开启 `EnforceCodeStyleInBuild`），以 IDE 提示 + code review 为准；`Migrations/*.cs` 标记 `generated_code`，不参与分析。

本规则只写 `.editorconfig` 无法表达的工程约定：

- 对外模型与实体分离：接口入参用 `Request`、出参用 `Response` / 共享出参模型，禁止直接返回 EF 实体。
- 异步优先：IO 相关方法使用 `async/await`，禁止同步阻塞（`.Result` / `.Wait()`）。
- 可空引用类型启用（各 `*.csproj` 已设 `<Nullable>enable</Nullable>`）；公共类型与公共方法写 XML 文档注释（中文）。

## 3. 项目结构

`backend/` 分 `src/App.Api` / `src/App.Core` / `src/App.Infrastructure` 与 `tests/App.Tests`（xUnit）；目录、文件位置与端内参照实现见 `.codebuddy/CONTEXT.md` §2（本文件不重复，避免两处维护漂移）。

- 依赖方向单向（禁止反向依赖）：`App.Api → App.Core`、`App.Api → App.Infrastructure`、`App.Infrastructure → App.Core`。

### 3.1 实现前最小读取集（强制）

> 通用四项（`CONTEXT.md` / 规格 / 本规则 / 本次目标文件）见 `AGENTS.md` §2.4；**端内参照实现（四件套 / 无参用例 / 分发与全局校验）与「改动面 → 必读 / 必改文件」对照表见 `.codebuddy/CONTEXT.md` §2**。禁止为"了解项目"做全仓探索或目录级遍历、禁止读与本功能无关的 Feature、禁止为"了解架构"通读 `Migrations/`（改表结构只看最近 1~2 个迁移的命名与写法）。

## 4. 分层架构（每 API 一个用例）

```
Controller → IMediator.Send(<Action>Request) → 已注册的 <Action>RequestHandler → Repository（接口在 App.Core）→ Repository 实现（EF Core）
```

### 4.1 用例结构

**一个 API 对应 `App.Core/Features/<Feature>/<Action>/` 下的一组文件**（`<Action>` 为用例名，如 `Login`），命名空间 `App.Core.Features.<Feature>.<Action>`，四件套：`<Action>Request.cs`（入参，与前端参数 camelCase 一一对应；属性 `init`、字符串默认 `string.Empty`，实现 `IRequest<TResponse>` 声明响应类型）/ `<Action>RequestValidator.cs`（FluentValidation 格式校验：必填 / 长度 / 格式 / 枚举 / 范围，长度区间正则按 §5.3 取常量，**禁止访问仓储 / 查库**——存在性、唯一性、状态流转等约束放 Handler）/ `<Action>RequestHandler.cs`（业务逻辑 + 查库约束 + 事务，**本用例唯一编排点**，实现 `IRequestHandler<TRequest, TResponse>`，构造只注入本用例所需依赖）/ `<Action>Response.cs`（出参，即 `data` 模型；跨用例复用者放 `Features/<Feature>/` 下，不放进用例目录）。

- **Handler 判据（强制）**：`Request → 实体`、`实体 → Response` 映射在 Handler 内完成，禁止暴露实体到 API；业务失败统一抛 `BusinessException`，错误码用 `Errors/ErrorCode` 常量（全局码见 `AGENTS.md` §4.2），功能专属业务码先在功能 `design.md` 定义。**不设 Service 层**：禁止新增参与业务编排的 `*Service`、不得引入聚合型 Handler，通用无状态技术组件按职责命名（如 `Auth/TokenService`）。
- **不接触 HTTP / 当前用户（强制）**：禁止在 Core 注入 `HttpContext` / 使用 `ControllerBase`；当前用户经 `Abstractions/ICurrentUser` 获取（实现由 `App.Api` 基于 ClaimsPrincipal 提供），id 解析统一用 `ICurrentUserExtensions.UserId()`，禁止内联 `Guid.TryParse`；审计字段（CreatedBy / UpdatedBy）由 Handler 随实体 / `operatorId` 参数传入，仓储不感知当前用户。
- 基准范例 `Features/Auth/Login`（四件套）、`Features/Users/GetCurrentUser`（空 `Request` 占位 + 复用共享出参，不定义 Validator），新增功能一比一照此组织；单测可直接测 Handler，也可经 `IMediator` 验证分发。

**用例不接触 HTTP / 当前用户（强制）**：禁止在 Core 注入 `HttpContext` / 使用 `ControllerBase`；当前用户经 `Abstractions/ICurrentUser` 获取（实现由 `App.Api` 基于 ClaimsPrincipal 提供），id 解析统一用 `ICurrentUserExtensions.UserId()`，禁止内联 `Guid.TryParse`；审计字段（CreatedBy / UpdatedBy）由 Handler 随实体 / `operatorId` 参数传入，仓储不感知当前用户。

### 4.2 中介分发与格式校验

- **中介分发（强制）**：Controller 只注入 `Abstractions/IMediator`，用 `await _mediator.Send(request)` 触发用例，禁止直接依赖或 `new` 具体 Handler。分发实现 `Mediation/Mediator`（自研简化 MediatR）：按请求运行时类型经 DI 解析已注册的 `IRequestHandler<TRequest,TResponse>` 并调用 `HandleAsync`。
- **格式校验全局统一（强制）**：由 `Mediator` 在分发前统一执行——`Send` 内按请求运行时类型解析已注册的 `IValidator<TRequest>` 并 `ValidateAsync`，失败抛 `BusinessException(ErrorCode.Validation, 首条错误信息)`，未注册校验器的请求（如空请求用例）自动跳过。Handler 不注入 / 不执行校验器，Controller 不做参数校验。
- **ASP.NET Core 默认校验只保留绑定层（强制）**：`Program` 必须 `ConfigureApiBehaviorOptions(o => o.SuppressModelStateInvalidFilter = true)` 关闭 `[ApiController]` 自动 400（RFC problem-details 违反 `AGENTS.md` §4.1，前端表现为「网络异常」）；同时必须全局注册 `Middleware/ModelStateValidationFilter`——关闭后动作方法仍会带非法模型执行（`[FromBody]` 请求对象为 null），由该过滤器在动作执行前抛 `BusinessException(ErrorCode.Validation, "参数错误")` 短路，原始错误只入日志（含字段与 traceId）不回传。**判据**：任何请求格式错误（反序列化失败、类型不匹配、路由 / Query 参数绑定失败）必须返回统一响应 `40000`，禁止出现 problem-details；禁止为承接绑定校验而在 Controller 或 Handler 内加 `ModelState` / DataAnnotations 检查。
- **路由约定（强制）**：统一 `/api/<资源复数>`（`AGENTS.md` §4.4）；动作用 HTTP 动词 + 资源 / 子资源路径（如 `PUT /api/users/{id}/status`），用例名不进路径，不在路径中暴露实现细节。
- **分页入参（强制）**：`page` ≥ 1、`pageSize` 默认 20、上限 100（`AGENTS.md` §4.3）；Validator 用 `InclusiveBetween` 等表达边界，越界由全局校验统一返回 `40000`。
- **路由参数不得从请求体绑定（强制）**：`[FromBody]` 直接绑定用例 Request 时（如 `Update<X>Request` 兼作中介请求），取自路由的 `Id` 必须是**可缺省**成员。标 `required` 后请求体缺 `id` 会被 `[ApiController]` 的 ModelState 过滤器短路成 RFC problem-details 400（前端表现为「网络异常」，动作方法不执行、全局异常中间件拿不到）。Controller 以路由值覆盖 `Id`，并保留 Validator 的 `NotEmpty` 兜底。

### 4.3 DTO 与读模型

- **DTO 映射统一约定（强制）**：
  - **正向（实体 / 读模型 → 出参 DTO）**：集中在 `Features/<Feature>/` 下的 `internal static class <Feature>DtoMapper`，**禁止在 Handler 内联 `new XxxDto` / 私有 `ToDto` 重复拼装**；方法名统一为 `To` + 目标 DTO 类型名（如 `UserDtoMapper.ToUserDetailDto`）；同一 DTO 的映射字段须一致，派生 / 计算字段（如低库存标记）在 Mapper 内计算；多个源类型映射同一 DTO 用重载区分。
  - **反向（Request → 实体）**：与正向区分方向，禁止与正向 `To` 前缀混用；默认在各用例 Handler 内联拼装，抽取工厂方法时统一 `From` + 源类型名，是否集中由各功能规格决定。
- **读模型约定（强制）**：读模型是**仓储出参契约**，与 `Entity`（持久化模型）、`DTO`（API 契约）分属三层：
  - **定位与形态**：只供仓储接口 ↔ Mapper 传递，禁止暴露到 API；形态固定为 `sealed record` + `required` + `init`（不可变），放 `App.Core/Abstractions/`，与仓储接口同层。
  - **创建判据**：仅当查询结果含**实体上没有的联查字段**（如 join 带出的 `CategoryName`）时才建；单表查询、字段与实体 1:1 的仓储直接返回实体（`AsNoTracking`）；主表 + 明细的复合结构用元组 `(Entity? Order, IReadOnlyList<ItemEntity> Items)` 返回，不为此新增读模型。
  - **命名**：`<实体><用途>`，用途取自查询场景（举例 `ProductDetail` / `ProductListItem`；新增域照此命名，**不在此登记**）。
  - **规格联动**：必须先在功能 `design.md` §3.1 的仓储接口表中定义（含联查说明）再落地；`Abstractions/` 类型清单变更时同步更新 `.codebuddy/CONTEXT.md`。

### 4.4 仓储与事务

- **Repository**：方法最小化（如 `GetByIdAsync`、`ExistsByEmailAsync`），只做数据访问、不做业务判定；仓储接口与 `IUnitOfWork` 定义在 `App.Core/Abstractions`，实现（EF Core）在 `App.Infrastructure`。
- **事务**：单一仓储的一次写操作由该仓储保证；一个用例内**跨多个仓储的写操作**必须用 `IUnitOfWork` 包裹：`BeginTransactionAsync` → 各仓储写 → `CommitAsync`；异常时 `RollbackAsync` 后重抛，禁止吞异常。

### 4.5 依赖注入注册

- `App.Core/DependencyInjection.AddCore` 显式注册无状态技术组件、`IMediator → Mediator`（一次）、各用例的 `IRequestHandler<TRequest, TResponse>` 与 `IValidator<Request>` 接口映射；`App.Infrastructure/DependencyInjection.AddInfrastructure` 注册 `IUnitOfWork` 与仓储实现。**新增用例必须同步注册**；禁止反射隐式扫描（`Send` 时按请求类型解析单个已注册实现不算扫描），禁止在 Controller 手动 `new` 依赖。

## 5. 数据库规范（PostgreSQL + EF Core）

### 5.1 表结构与迁移

- 所有表结构变更必须走 EF Core Migrations；禁止手写 DDL，禁止修改已应用的迁移文件。
- 业务代码中禁止裸 SQL；确需 EF Core 无法表达的场景，必须在规格中说明并经用户确认。
- 实体名 PascalCase 单数；表名默认与实体名一致，规格可另行定义。

### 5.2 时间字段一律 `DateTimeOffset`（强制）

- **实体 / DTO（Request / Response）/ 仓储方法签名**中的所有时间字段统一用 `DateTimeOffset`（可空用 `DateTimeOffset?`），**禁止 `DateTime`**（`DateTime.Kind` 在 JSON 序列化与跨层传递中会丢失，导致 UTC 与本地时间混用）。
- 取当前时间统一 `DateTimeOffset.UtcNow`；禁止 `DateTime.Now`，禁止直接调用 `DateTime.UtcNow`（确需 `DateTime` 值时用 `.UtcDateTime` 转换，不让裸 `DateTime` 进入业务层）。
- Npgsql 将其映射为 `timestamptz`，库内按 UTC 存储，接口出入参保留偏移量；类型改造若不影响 DDL，允许生成 Up/Down 为空的迁移，不要为"迁移有内容"去改列类型。
- 时间范围筛选入参为 `DateTimeOffset`（前端传 UTC ISO 串），偏移量显式，后端**不做时区推断 / 不做 UTC 归一化**；闭区间比较直接依赖 `timestamptz` 语义。
- **唯一例外**：第三方 API 只接受 `DateTime` 时（如 `JwtSecurityToken` 的 `notBefore` / `expires`），在调用边界显式 `.UtcDateTime` 转换并加注释（参照 `TokenService.Issue`）。
- 新增时间字段时同步更新功能 `design.md` 的数据模型表与表结构表。

### 5.3 字段约束单一来源，Validator 与数据规则一致（强制）

> 参照实现（**固定为 user 域，不随新增域扩张**）：`Entities/UserFieldConstraints.cs` + `Configurations/UserConfiguration.cs` + `Features/Users/**/*RequestValidator.cs`，一致性由 `tests/App.Tests/FieldValidationConsistencyTests.cs` 守护。

- 「数据规则」（长度 / 区间 / 正则 / 必填 / 可空）在代码侧**只在一处定义**：`App.Core/Entities/<实体>FieldConstraints.cs` 常量类。引用方必须使用该常量，**禁止硬编码字面量、禁止复制正则**：EF 配置（`HasMaxLength` / `IsRequired` / 索引等，在 `Persistence/Configurations/<实体>Configuration.cs`）与各用例 `RequestValidator` 均取常量。
- **同一字段在不同用例中的规则必须同源**，禁止分叉（如登录 / 创建 / 重置的密码区间都取 `PasswordMinLength` / `PasswordMaxLength`）；无对应表列但仍属业务规则的（明文密码区间、查询关键词长度上限）同样收敛到该常量类。
- **规格是事实源**：以 `specs/<序号>-<feature>/design.md` 的「数据模型 / 表结构」约束表与校验规则表为准；改动顺序 `design.md` → 常量类 → EF 配置 → 各 Validator → 一致性单测，代码与规格必须始终一致。
- 单测至少覆盖：① EF 实际 `HasMaxLength` == 常量；② 同一字段在多个用例 Validator 中「边界值通过 / 越界拒绝」一致；③ 查询关键词长度上限 == 该查询**实际匹配列**的最大列长（按列 `Contains`，超列长不可能命中）——**跨域不照抄数值**，各域取值见对应 `design.md`。
- 新增字段 / 修改约束时，上述五处缺一即视为未完成，不得勾选规格 `tasks.md` 中对应任务。

## 6. 认证（JWT）

- 使用 JWT 认证；token 有效期与 claims 在各功能规格中定义。
- `App.Api` 中配置 JWT 校验（中间件 / 授权）与登录等白名单放行；白名单范围、`40100` 响应、token 签发与携带等全局约定见 `AGENTS.md` §4.6。
- 签名密钥来源见 §8（遵循 `AGENTS.md` §7 敏感配置原则）。

## 7. 日志与可观测性

- 日志集成 NLog 并对接默认 `ILogger<T>`；业务代码只注入 `ILogger<T>`，禁止直接调用 NLog。配置放 `App.Api`，输出控制台 + 滚动文件，布局含 traceId（要求见 `AGENTS.md` §5）。
- 日志级别：**dev / prod 均 Info 起**，框架日志（`Microsoft.*` / `System.*`）单独以 Warn 起压噪（见 `nlog.config`）——不靠整体降级减少噪音，否则会连带丢弃 Info 级业务日志。
- 可观测性集成 OpenTelemetry（Tracing + Metrics）：在 `App.Api` 引入 SDK，自动埋点 ASP.NET Core 与 EF Core；导出目标按环境配置（dev 可不导出，prod 导出至 collector）。

## 8. 环境配置

- 环境划分与配置原则见 `AGENTS.md` §7（仅 dev / prod）；实现上通过 `ASPNETCORE_ENVIRONMENT` + `appsettings.{Environment}.json` 区分，`appsettings` 只放非敏感默认值与非生产凭据（dev 允许连接串明文）；代码内一律禁止硬编码凭据。
- 后端敏感项清单：数据库连接串、JWT 密钥、OTel 导出端点。

## 9. 测试

- 单元测试项目 `tests/App.Tests`（xUnit）；覆盖重点（RequestHandler 业务逻辑）与测试门槛见 `AGENTS.md` §6。命名 `{类名}_{方法名}_{场景}`，RequestHandler 的公共方法必须有对应单测。
- 仓储与用例测试**优先用行为型假实现**（举例：采购 / 销售的 `*TestDoubles`；新增域就地实现，**不在此登记**）；EF Core InMemory 仅用于不依赖关系型特性的简单读场景——**InMemory 不支持 `ExecuteUpdateAsync` / 行锁**，依赖这类写操作的仓储与生命周期测试必须用行为型假实现规避。
- 测试工程对 `CA2000` 采用**汇编级抑制**（`tests/App.Tests/GlobalSuppressions.cs`）：`DbContext` / `UnitOfWork` 在测试中跨调用复用，逐个 `using` 会破坏测试语义；`src/` 仍保持 `error`，**不要删除该抑制文件**。
- **测试用例要求（强制）**：凡涉及后端改动，交付前必须为新增 / 变更的业务逻辑补充正常分支 + 异常分支用例；无对应用例视为未完成，不得勾选规格 `tasks.md` 中对应任务。

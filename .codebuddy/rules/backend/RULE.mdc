---
description: 后端实现规则。开发或修改 backend/ 下的 .NET 8 / ASP.NET Core / EF Core 代码时应用，含编码约定、分层架构、数据库规范、认证、日志、环境配置与测试约定。
alwaysApply: false
enabled: true
---

# 后端实现规则

> 总则见根目录 `AGENTS.md`（SDD 工作流、API 契约、错误码、通用约定与测试门槛）。本文件只写后端专属实现约定，总则已规定的条款不再重复列出；确需引用时标注章节（如 `AGENTS.md` §4.5 / §6）。
> 开发功能前：先读 `specs/<feature>/` 规格（`AGENTS.md` §2.2），再按本文件实现。

## 1. 技术栈

- .NET 8 + ASP.NET Core Web API
- EF Core（Npgsql 提供程序）+ PostgreSQL
- FluentValidation（格式校验，规则与用例同目录）

## 2. 编码约定

- 遵循 C# 语言规范：4 空格缩进、Allman 大括号、文件内 `using` 排序（System 在前）。
- 命名：类 / 方法 / 属性 PascalCase，私有字段 `_camelCase`，接口以 `I` 开头。
- 对外模型与实体分离：接口入参用 `Request`、出参用 `Response` / 共享出参模型，禁止直接返回 EF 实体。
- 异步优先：IO 相关方法使用 `async/await`，禁止同步阻塞（`.Result` / `.Wait()`）。
- 可空引用类型启用；公共类型与公共方法写 XML 文档注释（中文）。

## 3. 项目结构

```
backend/
├── App.sln
├── src/
│   ├── App.Api/            # Controller、中间件、ICurrentUser 实现、全局异常处理、统一响应
│   ├── App.Core/           # 业务核心：Features（用例）、Abstractions（仓储接口 / IUnitOfWork / IRequest / IRequestHandler / IMediator）、Entities、公共组件
│   └── App.Infrastructure/ # DbContext、Repository 实现、UnitOfWork 实现
└── tests/
    └── App.Tests/          # 单元测试（xUnit）
```

- 依赖方向单向（禁止反向依赖）：
  - `App.Api` → `App.Core`，`App.Api` → `App.Infrastructure`
  - `App.Infrastructure` → `App.Core`
- 目录内文件的完整清单以 `.codebuddy/CONTEXT.md` §2 为准（本文件只保留骨架，避免两处维护漂移）。

## 4. 分层架构（每 API 一个用例）

```
Controller → IMediator.Send(<Action>Request) → 已注册的 <Action>RequestHandler → Repository（接口在 App.Core）→ Repository 实现（EF Core）
```

### 4.1 用例结构

**一个 API 对应 `App.Core/Features/<Feature>/<Action>/` 下的一组文件**（`<Action>` 为用例名，如 `Login`、`CreateUser`），命名空间 `App.Core.Features.<Feature>.<Action>`：

| 文件 | 职责 |
|---|---|
| `<Action>Request.cs` | 入参模型，与前端请求参数 camelCase 一一对应；属性用 `init`，字符串默认 `string.Empty`；实现标记接口 `IRequest<TResponse>`（声明响应类型） |
| `<Action>RequestValidator.cs` | 数据格式校验（必填、长度、格式、枚举、范围等），基于 FluentValidation |
| `<Action>RequestHandler.cs` | 业务逻辑 + 查库约束 + 事务，本用例的唯一编排点 |
| `<Action>Response.cs` | 出参模型（即 `data` 的模型） |

以脚手架为**基准范例**：`Features/Auth/Login`（Request / RequestValidator / RequestHandler / Response 四件套）、`Features/Users/GetCurrentUser`（无参用例以空 `Request` 占位 + 复用共享出参 `UserDto`）；新增功能照此结构一比一组织。

用例结构细则：

- **Request**：实现标记接口 `IRequest<TResponse>`（声明响应类型），供中介分发时确定响应类型。
- **RequestValidator（强制）**：只做格式层校验，禁止访问仓储 / 查库；需要查询数据的约束（存在性、唯一性、状态流转等）一律放到 `RequestHandler` 中判断。字段长度 / 区间 / 正则必须取自 §5.3 的字段约束单一来源，禁止硬编码（须与"数据规则"一致）。
- **RequestHandler（强制）**：本用例唯一业务编排点，直接依赖仓储接口与无状态技术组件；**不设 Service 层**——禁止新增**参与业务编排**的 `*Service`（如 `OrderService`），也不得再引入聚合型 Handler；通用无状态技术组件不受此限，按职责命名（如 `Auth/TokenService`、`PasswordHasher`）。实现 `IRequestHandler<TRequest, TResponse>`（唯一公共方法 `Task<TResponse> HandleAsync(TRequest, CancellationToken)`），构造只注入本用例所需依赖；单测可直接测该实现，也可经 `IMediator` 验证分发。
- 无请求参数的用例（如"获取当前用户"）也定义一个空 `Request` 模型（如 `GetCurrentUserRequest`）占位以统一签名，但不定义 `Validator`；纯内存用例在实现内同步计算后 `return Task.FromResult(result)`，无需 `async` 关键字。
- **用例不接触 HTTP（强制）**：禁止在 Core 注入 `HttpContext` / 使用 `ControllerBase`；需要"当前用户"的用例注入 `App.Core/Abstractions/ICurrentUser`（实现由 `App.Api` 基于 ClaimsPrincipal 提供）。
- **当前用户与审计字段（强制）**：id 解析统一用 `Abstractions/ICurrentUserExtensions.UserId()` 扩展方法（claims 缺失 / 非法返回 `null`），禁止内联 `Guid.TryParse` 或私有解析方法；审计字段（CreatedBy / UpdatedBy）一律由 Handler 经 `ICurrentUser` 获取后随实体 / `operatorId` 参数传入，仓储不注入 `ICurrentUser`、不感知当前用户。
- **业务失败（强制）**：统一抛 `BusinessException`，错误码使用 `Errors/ErrorCode` 常量（全局码见 `AGENTS.md` §4.2）；功能专属业务码（如登录失败 `40001`）必须先在各功能 `design.md` 定义再使用。
- **映射**：`Request → 实体` 与 `实体 → Response` 在 `RequestHandler` 内完成，禁止把实体直接返回 Controller / 暴露到 API。出参复用：跨用例 / 多次复用的出参模型（如 `UserDto`）放 `Features/<Feature>/` 下，不放进单个用例目录。

### 4.2 中介分发与格式校验

- **中介分发（强制）**：Controller / 中间层只注入 `App.Core/Abstractions/IMediator`，用 `await _mediator.Send(request)` 触发用例，禁止直接依赖或 `new` 具体 Handler。分发实现 `App.Core/Mediation/Mediator`（自研简化 MediatR）：按请求运行时类型经 DI 解析已注册的 `IRequestHandler<TRequest,TResponse>` 后调用其 `HandleAsync`。
- **格式校验全局统一（强制）**：格式校验由 `Mediator` 在分发到 Handler 之前统一执行——`Send` 内按请求运行时类型经 DI 解析已注册的 `IValidator<TRequest>` 并 `await ValidateAsync`，失败抛 `BusinessException(ErrorCode.Validation, 首条错误信息)`，未注册校验器的请求（如空请求用例）自动跳过。`RequestHandler` 不再注入 / 执行校验器，`HandleAsync` 只写业务逻辑与查库约束；Controller 不做参数校验。
- **路由约定（强制）**：Controller 路由统一 `/api/<资源复数>`（`AGENTS.md` §4.4）；动作用 HTTP 动词 + 资源 / 子资源路径（如 `GET /api/categories/paged`、`PUT /api/users/{id}/status`），用例名不进路径，不在路径中暴露实现细节。
- **分页入参（强制）**：`page` ≥ 1、`pageSize` 默认 20、上限 100（`AGENTS.md` §4.3）；Validator 用 `InclusiveBetween` 等表达该边界，越界由全局校验统一返回 `40000`。

### 4.3 DTO 与读模型

- **DTO 映射统一约定（强制）**：
  - **正向（实体 / 读模型 → 出参 DTO）**：凡同一功能存在出参映射，集中在 `Features/<Feature>/` 下的 `internal static class <Feature>DtoMapper` 中实现，**禁止在 Handler 内联 `new XxxDto` / 私有 `ToDto` 重复拼装**；方法名统一为 `To` + 目标 DTO 类型名（全部以 `Dto` 结尾），如 `UserDtoMapper.ToUserDetailDto` / `ProductDtoMapper.ToProductDto` / `PurchaseDtoMapper.ToPurchaseOrderListItemDto`；同一 DTO 的映射字段须一致，派生 / 计算字段（如低库存标记）在 Mapper 内计算（可提私有方法）；多个源类型映射同一 DTO 时用重载区分（如读模型重载、实体 + 上下文重载）。
  - **反向（入参 Request → 实体）**：与正向区分方向，若抽取工厂方法，方法名统一为 `From` + 源类型名（如 `Partner.FromCreatePartnerRequest`，对应正向 `ToPartnerDto`），禁止与正向 `To` 前缀混用。当前反向映射均为各用例 Handler 内联的对象初始化拼装（实体构造各异），暂不强制集中；后续确需抽取时（实体工厂方法或 Mapper 的 `From` 方法）须遵守 `From` 前缀约定。
- **读模型约定（强制）**：读模型（Read Model）是**仓储出参契约**，与 `Entity`（持久化模型）和 `DTO`（API 契约）分属三层：
  - **定位与形态**：表达"查询结果长什么样"，只供仓储接口 ↔ Mapper 之间传递，禁止暴露到 API。形态固定为 `sealed record` + `required` + `init`（不可变），放在 `App.Core/Abstractions/` 下，与仓储接口同层。
  - **创建判据**：仅当查询结果含**实体上没有的联查字段**（如 join 带出的 `CategoryName` / `StockQuantity`）时才建读模型；单表查询、字段与实体 1:1 无额外信息的，仓储直接返回实体（`AsNoTracking`）；主表 + 明细的复合结构用元组 `(Entity? Order, IReadOnlyList<ItemEntity> Items)` 返回，不得为此新增读模型。
  - **命名**：`<实体><用途>`，用途取自查询场景，如 `ProductDetail` / `ProductListItem` / `ProductPickItem` / `InventoryItem`。
  - **规格联动**：读模型必须先在功能 `design.md` §3.1 的仓储接口表中定义（含联查说明），再落地为代码；`Abstractions/` 类型清单变更时同步更新 `.codebuddy/CONTEXT.md`。

### 4.4 仓储与事务

- **Repository**：方法最小化（如 `GetByIdAsync`、`ExistsByEmailAsync`），只做数据访问、不做业务判定；仓储接口与 `IUnitOfWork` 定义在 `App.Core/Abstractions`，实现（EF Core）在 `App.Infrastructure`。
- **事务**：单一仓储的一次写操作由该仓储保证；一个用例内**跨多个仓储的写操作**必须用 `IUnitOfWork` 包裹：`BeginTransactionAsync` → 各仓储写 → `CommitAsync`；异常时 `RollbackAsync` 后重抛，禁止吞异常。

### 4.5 依赖注入注册

- `App.Core/DependencyInjection.AddCore` 显式注册无状态技术组件、`IMediator → Mediator`（一次）、各用例的接口映射（`AddScoped<IRequestHandler<LoginRequest, LoginResponse>, LoginRequestHandler>()`）与 `IValidator<Request>`；`App.Infrastructure/DependencyInjection.AddInfrastructure` 注册 `IUnitOfWork` 与仓储实现（Scoped）。新增用例必须同步注册，禁止反射隐式扫描（`Send` 时按请求类型解析单个已注册实现不属于扫描），禁止在 Controller 手动 `new` 依赖。

## 5. 数据库规范（PostgreSQL + EF Core）

### 5.1 表结构与迁移

- 所有表结构变更必须走 EF Core Migrations；禁止手写 DDL，禁止修改已应用的迁移文件。
- 业务代码中禁止裸 SQL；确需 EF Core 无法表达的场景，必须在规格中说明并经用户确认。
- 实体名 PascalCase 单数；表名默认与实体名一致，规格可另行定义。

### 5.2 时间字段一律 `DateTimeOffset`（强制）

- **实体 / DTO（Request / Response）/ 仓储方法签名**中的所有时间字段统一用 `DateTimeOffset`（可空用 `DateTimeOffset?`），**禁止 `DateTime`**。理由：`DateTime.Kind` 在 JSON 序列化与跨层传递中会丢失，导致 UTC 与本地时间混用；`DateTimeOffset` 自带偏移量，语义明确。
- 取当前时间统一 `DateTimeOffset.UtcNow`；禁止 `DateTime.Now`，禁止直接调用 `DateTime.UtcNow`（确需 `DateTime` 值的场景见下条边界例外，用 `.UtcDateTime` 转换得到，不让裸 `DateTime` 进入业务层）。
- Npgsql 将 `DateTimeOffset` 映射为 `timestamptz`，库内按 UTC 存储，接口出入参保留偏移量。类型改造若不影响 DDL，允许生成 Up/Down 为空的迁移，不要为了"迁移有内容"去改列类型。
- 时间范围筛选入参为 `DateTimeOffset`（前端传 UTC ISO 串），偏移量显式，后端**不做时区推断 / 不做 UTC 归一化**；闭区间比较直接依赖 `timestamptz` 语义。
- **唯一例外**：第三方 API 只接受 `DateTime` 时（如 `JwtSecurityToken` 的 `notBefore` / `expires`），在调用边界显式 `.UtcDateTime` 转换并加注释说明（参照 `TokenService.Issue`）。
- 新增时间字段时同步更新各功能 `design.md` 的数据模型表与表结构表（§5.3 规格侧事实源）。

### 5.3 字段约束单一来源，Validator 与数据规则一致（强制）

> 参照实现：`App.Core/Entities/UserFieldConstraints.cs` + `Persistence/Configurations/UserConfiguration.cs` + `Features/Users/**/*RequestValidator.cs`，一致性由 `tests/App.Tests/FieldValidationConsistencyTests.cs` 守护。

- 「数据规则」（字段长度 / 区间 / 正则 / 必填 / 可空）在代码侧**只在一处定义**：`App.Core/Entities/<实体>FieldConstraints.cs` 常量类（如 `UserFieldConstraints`）。引用方必须使用该常量，**禁止硬编码字面量、禁止复制正则**：
  - EF 实体配置（`HasMaxLength` / `IsRequired` / `HasColumnType` / 索引等）→ `App.Infrastructure/Persistence/Configurations/<实体>Configuration.cs`；
  - 各用例 `RequestValidator` 的长度 / 区间 / 正则规则。
- **同一字段在不同用例中的规则必须同源**，禁止分叉：例如登录、创建、重置的密码区间必须都取 `PasswordMinLength` / `PasswordMaxLength`，不得某个用例写成 ≤128。无对应表列但仍属业务规则的（如明文密码区间、查询关键词长度上限）同样收敛到该常量类。
- **规格侧对应条款**是 `specs/<feature>/design.md` 的「数据模型 / 表结构」约束表与校验规则表，**规格是事实源**：改动顺序为 `design.md` → 常量类 → EF 配置 → 各 Validator → 一致性单测，代码与规格必须始终一致。
- 必须由单测守护一致性，至少覆盖：
  1. EF 模型实际 `HasMaxLength` == 常量（防配置与常量分叉）；
  2. 同一字段在多个用例的 Validator 中「边界值通过 / 越界拒绝」行为一致；
  3. 查询类关键词的长度上限 == 该查询**实际匹配列**的最大列长（关键词按列做 `Contains`，超过列长不可能命中）；**跨域不照抄数值**：搜 `Name` 的域取 `NameMaxLength`（如分类取 20），搜多列的取其中较大者（如商品搜 `Code`/`Name` 取 50），单据搜 `OrderNo` 取 20。
- 新增字段 / 修改约束时，上述五处缺一即视为未完成，不得勾选规格 `tasks.md` 中对应任务。

## 6. 认证（JWT）

- 使用 JWT 认证；token 有效期与 claims 在各功能规格中定义。
- `App.Api` 中配置 JWT 校验（中间件 / 授权）与登录等白名单放行；白名单范围、`40100` 响应、token 签发与携带等全局约定见 `AGENTS.md` §4.6。
- 签名密钥来源见 §8（遵循 `AGENTS.md` §7 敏感配置原则）。

## 7. 日志与可观测性

- 日志集成 NLog 并对接默认 `ILogger<T>`（Microsoft.Extensions.Logging）；业务代码只注入使用 `ILogger<T>`，禁止直接调用 NLog。
- NLog 配置放在 `App.Api`，输出控制台 + 滚动文件；日志级别：**dev / prod 均 Info 起**（prod 同样保留业务日志），框架日志（`Microsoft.*` / `System.*`）单独以 Warn 起压噪（见 `nlog.config`）——不靠整体降到 Warning 来减少噪音，否则会连带丢弃 Info 级业务日志。日志上下文含 traceId 的要求见 `AGENTS.md` §5。
- 可观测性集成 OpenTelemetry（Tracing + Metrics）：在 `App.Api` 引入 SDK，自动埋点 ASP.NET Core 与 EF Core；导出目标按环境配置（dev 可不导出，prod 导出至 collector）。

## 8. 环境配置

- 环境划分与配置原则见 `AGENTS.md` §7（仅 dev / prod）；实现上通过 `ASPNETCORE_ENVIRONMENT` + `appsettings.{Environment}.json` 区分，`appsettings` 只放非敏感默认值与非生产凭据（dev 允许连接串明文，例外范围见 `AGENTS.md` §7）；代码内一律禁止硬编码凭据。
- 后端敏感项清单：数据库连接串、JWT 密钥、OTel 导出端点。

## 9. 测试

- 单元测试项目：`tests/App.Tests`（xUnit）；覆盖重点（RequestHandler 业务逻辑）与测试门槛见 `AGENTS.md` §6。
- 仓储与用例测试**优先用行为型假实现**（参照 `PurchaseOrderTestDoubles` / `SalesOrderTestDoubles` 及其生命周期测试）；EF Core InMemory 提供程序仅用于不依赖关系型特性的简单读场景。
- **InMemory 限制（强制）**：InMemory **不支持 `ExecuteUpdateAsync` / 行锁等关系型特性**，依赖这类写操作的仓储与生命周期测试必须用行为型假实现规避。
- 测试命名：`{类名}_{方法名}_{场景}`；RequestHandler 的公共方法必须有对应单测。
- **测试用例要求（强制）**：凡涉及后端改动的功能，交付前必须为改动新增 / 变更的业务逻辑补充正常分支 + 异常分支的测试用例；无对应测试用例视为未完成，不得勾选规格 `tasks.md` 中对应任务。
